# QA-20260902-01 — головна сітка пар перед дозволеним релізом

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`, `.agents/skills/qa-batch/SKILL.md`, `info/qa/process.md`, `info/qa/report-template.md`, `info/tasks.md` (VID-026 і VID-028) та `info/home-period-grid.md`. Ти вже QA-агент: не делегуй цей QA іншому агенту.

## Контекст

- Тригер: підготовка прямо дозволеного користувачем production deployment і повторної прив’язки `web-dev.pp.ua`.
- Пакет: `QA-20260902-01`.
- Задачі: продуктова VID-026; для VID-028 перевірити лише готовність релізного обсягу й відсутність заборонених файлів. Deployment, домен і post-deploy smoke виконує основний агент тільки після твого актуального `PASS`.
- Первинні запити й уточнення: точні цитати та критерії з `info/tasks.md#vid-026--головна-сітка-розкладу-за-парами` і `info/tasks.md#vid-028--qa-deployment-і-повторна-привязка-домену`.
- База: commit `ef12378` — останній стан `origin/main` перед VID-026.
- Перевірюваний стан: `ef12378` + усі tracked/untracked зміни VID-026, точний запис VID-028 і це доручення. Самостійно зафіксуй перелік та SHA-256 відбиток файлів без env/секретів.
- Очікувані продуктові файли: `app/(public)/page.tsx`, `app/globals.css`, `components/home-period-grid.tsx`, `components/home-schedule-board.tsx`, `components/home-schedule-board.module.css`, `lib/schedule-calendar/presentation.ts`, `lib/schedule-calendar/presentation.test.ts`, `info/home-period-grid.md`, `info/README.md`, `info/tasks.md`.
- Дозволена QA-документація: `info/qa/reports/QA-20260902-01.md` і QA-поля VID-026/028. Не включай звіт до відбитка продуктового коду.
- Виключення: VID-027 — відкладена задача, HTML ще надходять; не реалізовувати, не тестувати як готову фічу й не включати дані імпорту. Попередні опубліковані задачі — лише близька регресія за потреби.
- Середовище: локальний test harness може мати production-подібні секрети; не виводь значення. Для браузера використовуй безпечні read-only публічні сценарії або ізольовані fixtures. Жодних production-записів чи міграцій.
- Модель: `gpt-5.6-terra`, reasoning `high`, згідно з політикою звичайного UI-пакета.
- Режим: лише QA; продукт не редагувати, push/deploy/domain/DNS/production migration заборонені.

## Обов’язковий обсяг

1. Самостійно класифікуй VID-026 і готовність VID-028; зістав фактичний diff з кожним критерієм, не довіряючи позначці «реалізовано».
2. Виконай `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build`. Зафіксуй exit codes, кількість виконаних / пропущених тестів; пропущені DB suites не називай пройденими. DB integration для цієї presentation-only зміни виконуй лише якщо самостійно виявиш ризик даних; інакше обґрунтуй `N/A`.
3. Для реального UI прочитай повні інструкції доступного browser/Playwright skill і `.agents/skills/ui-spacing-review/SKILL.md`. Перевір головну на 1440, 820 і 390 px: сітка саме за номерами пар; усі активні й порожні слоти; паралельні заняття; видимі предмет/викладач/аудиторія/групи/тип; поточний стан і верхня шкала; today / інша / некоректна дата; previous/next; прихований date picker; keyboard/focus; reduced motion; console; тільки локальний horizontal scroll і відсутність document overflow.
4. Зафіксуй фактичні browser-вимірювання відступів, вирівнювання, щільності й overflow на трьох ширинах через `ui-spacing-review`. Не перетворюй QA на новий редизайн.
5. Перевір склад релізу: без env/секретів/generated/Playwright artifacts і без реалізації VID-027. Перевір, що Vercel link вказує на `vidmitka`, але не змінюй віддалений стан.
6. Створи `info/qa/reports/QA-20260902-01.md`, онови лише QA-поля/результат VID-026 і readiness VID-028 у `info/tasks.md`. Поверни `PASS`, `FAIL` або `BLOCKED`, точний стан коду, дефекти/обмеження й чи відкритий gate для релізу.

Продукт не виправляй, не коміть, не пуш, не деплой і не торкайся домену. Якщо знайдеш дефект, поверни його основному агенту з пріоритетом і доказом.
