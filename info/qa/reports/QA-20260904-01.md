# QA-звіт: QA-20260904-01

## Контекст і межі

- Тригер, дата й час: авторизована підготовка production-релізу («задеплой»), 04.09.2026 09:39–09:45 EEST.
- ID QA-агента, модель, reasoning; причина вибору/заміни: `/root/qa_vid046`, незалежний QA-субагент; точні label моделі й reasoning не надані метаданими цієї сесії, заміни моделі не було.
- Задачі пакета: `VID-046`.
- Базовий commit і перевірений commit / dirty worktree: база production `3a3697ef0c2fdfc945542ef94aa0cb7488783e10`; перевірено той самий `HEAD` із dirty scope.
- Файли та відбиток перевіреного обсягу (без секретів):
  - `app/globals.css` — `F82863DE38D5842DB32244FC607A641867E40717AC897DAC864CB05C58668C96`.
  - `components/public-schedule-explorer.module.css` — `E6B15FB4E7726EFC45D6B92F03E18173254827991E366C3679F7D3F2E8D988C2`.
  - `info/public-schedule-responsive-layout.md` — `AAB43DB80B1B0E603C0C8CED093230D8918281CB2F2556E28587DD3FC3D8F6FA`.
  - `info/tasks.md` перед записом QA-результату — `489B75937D7F325DFBD1B51170E40308E3AB546FDC22D51B85D35CBDB09F15B9`.
  - Незалежний product-code fingerprint: перші два CSS-файли; handoff і цей QA-звіт до нього не входять. `git diff --check` пройшов.
- Середовище й підтвердження ізоляції: локальний Next dev на `localhost:3000`; відкрито лише публічний `/schedule`, виконано тільки читання DOM/console та read-only URL-навігацію. Не виконувалися reimport, мутації, міграції чи production-операції.
- Що виключено та чому: завершені `VID-001`–`VID-045`, DB integration harness (CSS-only scope не змінює доступ до БД, дані, ролі або server actions), handoff і QA-звіт. Під час запуску `next dev` з'явилася стороння зміна `next-env.d.ts`; її не редагували, вона не входить у scope/fingerprint VID-046.

## Класифікація і план

| Задача | Тип, визначений QA | Підстава | Ризик | Сценарії та регресії |
| --- | --- | --- | --- | --- |
| VID-046 | Фіча (responsive layout / використання viewport) | Запит додає повноширинне компонування, а не відновлює порушений контракт. | P1: залишений `max-width`, некоректні поля або глобальний overflow на вузьких екранах. | Зіставити CSS із критеріями; typecheck, unit tests, build; read-only browser `/schedule` на 1440/820/390/1920; виміри header/workspace, overflow, ширини контролів; console, Tab/focus, Combobox + Escape, Enter-навігація дати й Back. |

## Результати критеріїв

| Задача / критерій | Перевірка або команда | Очікувано | Фактично / доказ | PASS / FAIL / BLOCKED / NOT_RUN / N/A |
| --- | --- | --- | --- | --- |
| VID-046: scope реалізації | Diff і `rg '1480px|1480'` у двох CSS-файлах та design note | Прибрати лише desktop max-width, не змінити дані/контроли. | `min(..., 1480px)` замінено на `calc(100% - 32px)` і tablet `calc(100% - 24px)`; у перевіреному scope посилань на 1480 немає. Diff містить тільки 2 CSS-рядки та документацію/ledger. | PASS |
| VID-046: desktop 1440 | Реальний браузер, viewport 1440×1000 | Header і workspace: ширина client viewport − 32px, поля 16px; без document overflow. | clientWidth 1425 (15px scrollbar); header і workspace clientWidth 1393, left 16px, right 1408.8px; document scrollWidth 1425. Панель і сітка вирівняні з workspace. | PASS |
| VID-046: tablet 820 | Реальний браузер, viewport 820×1000 | Header і workspace: ширина client viewport − 24px, поля 12px; без document overflow. | clientWidth 805; header і workspace clientWidth 781, left 12px, right 792.8px; document scrollWidth 805. | PASS |
| VID-046: mobile 390 | Реальний браузер, viewport 390×844 | Чинний full-width workspace; overflow сторінки відсутній. | clientWidth/workspace 375px, workspace left 0px; document scrollWidth 375. Header лишив чинні 16px поля (343px); вкладки днів мають лише власний horizontal scroll: clientWidth 375, scrollWidth 468, `overflow-x: auto`. | PASS |
| VID-046: широкий viewport | Реальний браузер, viewport 1920×1080 | Робоча область не обмежена 1480px і корисно використовує ширину. | clientWidth 1905; header і workspace clientWidth 1873, left 16px, right 1888.8px; 1873px > 1480px. document scrollWidth 1905. | PASS |
| VID-046: інтеракція та доступність | Read-only browser flow на `/schedule` | Клавіатурні основні контроли, пошук, Escape й URL-навігація не регресують. | Всі основні дата-контроли мають 44px висоту; input пошуку 42px у 44px Combobox. Tab дає видимий solid focus; trigger відкриває список викладачів, Escape закриває його та лишає focus на trigger. Enter на «Наступний день» перейшов до `/schedule?date=2026-09-05`; Back повернув `/schedule`. | PASS |
| VID-046: console і артефакти | Browser dev logs; `git diff --check` | Немає нових warning/error або whitespace-помилок. | error/warn console entries: 0; `git diff --check`: exit 0. | PASS |
| Обов'язкові quality checks | `pnpm exec tsc --noEmit` | Exit 0. | Exit 0. | PASS |
| Обов'язкові quality checks | `pnpm test` | Exit 0; результати фіксуються фактично. | Exit 0; Vitest: 10 files passed, 1 skipped; 77 tests passed, 9 skipped. Skipped DB/specialized сценарії не зараховувалися як пройдені й не релевантні CSS-only scope. | PASS |
| Обов'язкові quality checks | `pnpm build` | Exit 0 production build. | Exit 0; Next.js 16.3.3 compiled, typechecked, згенерував 19/19 static pages. | PASS |
| DB integration | `info/testing/attendance-runner.mjs` | Перевірити, якщо змінено DB-поведінку. | N/A: diff не змінює схему, queries, server actions, дані, права чи бізнес-правила; середовище може підключати production DB, тому мутаційний harness не запускався. | N/A |

## Потребує виправлень

Підтверджених дефектів не знайдено.

## Неперевірене й обмеження

- Browser API не підтримав очікування `networkidle`; результат переходу підтверджено наступним повним accessibility snapshot і URL, тому це не є пропущеним сценарієм чи дефектом застосунку.
- Скриншоти не створювалися: доказами spacing review є фактичні `getBoundingClientRect()`, `clientWidth`/`scrollWidth` та browser accessibility state без персональних даних у файлах.
- Локальний dev використовує `.env.local`; browser QA обмежено публічним read-only шляхом, тому production-дані не змінювалися.

## Прибирання

- Запущений для QA локальний `pnpm dev` буде зупинено після фіксації звіту.
- Вкладка та viewport override браузера створені тільки для QA; viewport скинуто до системного. Артефактів і тестових схем не створювалося.
- `next-env.d.ts` не чіпали: це стороння, поза scope зміна, породжена dev tooling.

## Висновок

- Результат пакета: `PASS`.
- Підтверджені задачі: `VID-046`.
- Задачі, що потребують виправлень: немає.
- Заблоковані або неперевірені критерії: немає обов'язкових; DB integration має обґрунтований `N/A` для CSS-only scope.
- Актуальність для конкретного стану коду: тільки для `HEAD 3a3697e` і задокументованого dirty fingerprint вище. Будь-яка зміна CSS/критеріїв після цього звіту потребує нового QA.
- QA-gate для релізу: відкритий для цього scope; QA не виконує commit, push або deployment.
