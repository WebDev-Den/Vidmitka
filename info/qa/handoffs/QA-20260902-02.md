# QA-20260902-02 — нова концепція розкладу VID-029

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`,
`.agents/skills/qa-batch/SKILL.md`, `.agents/skills/ui-spacing-review/SKILL.md`,
`info/qa/process.md`, `info/qa/report-template.md` та VID-029 у `info/tasks.md`.
Ти вже QA-агент: не делегуй цей QA іншому агенту.

- Тригер: прямий запит користувача «перевір чи все працює і задеплой».
- Пакет: `QA-20260902-02`.
- Задача: `VID-029`.
- Первинний запит: специфікація з 17 розділів у
  `C:/Users/WebDev/.codex/attachments/8f069af9-bb51-494c-ba5f-996d9dbda266/pasted-text.txt`;
  уточнення джерела — `D:/GItLab/Vidmitka/data/teacher-schedule-lessons.json`.
- База: commit `5a632ad851f1505b3cc0965d4fe529a1f0d0bf94` (останній опублікований стан перед VID-029).
- Перевірюваний стан: поточний `HEAD` плюс весь dirty/untracked обсяг VID-029,
  включно з міграцією 014, `app/admin/**`, `app/api/**`, `lib/schedule-v2/**`,
  `lib/schedule-import-v2/**`, публічними сторінками, видаленням legacy-коду,
  `data/teacher-schedule-lessons.json`, `data/teacher-schedules-import.json` і документацією.
- Виключення: production deployment, push, production migration, робочі дані та env-секрети.
- Середовище: локальний workspace; локальний env може вказувати на production, тому DB-перевірки
  проводь лише в доведено ізольованій тимчасовій схемі/БД. Якщо безпечну ізоляцію неможливо
  підтвердити — постав відповідний критерій `BLOCKED`, не змінюй робочі таблиці.
- Режим: лише QA; продукт не редагувати, push/deploy/production migration заборонені.
- Дозволені записи: QA-звіт, QA-поля `info/tasks.md`, артефакти й необхідні тести в межах VID-029.

Самостійно класифікуй запит і склади один ризик-орієнтований план. Обов’язково:

1. Зістав усі критерії специфікації з фактичним кодом, не довіряючи статусу «реалізовано».
2. Виконай `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build` і зафіксуй exit codes.
3. Перевір міграцію 014 на чистій ізольованій схемі та повторне застосування, якщо середовище безпечне.
4. Перевір parser на фактичному JSON із 431 записом, preview, idempotency, rollback критичної помилки
   та відсутність часткових записів у доведеній ізоляції.
5. Перевір auth: немає public login/signup; `/admin` і всі `/admin/**` захищені; legacy teacher
   не отримує доступу; login має generic errors, throttle, secure session/logout.
6. Перевір CRUD довідників, базового розкладу, конфлікти, тижні, винятки, перенесення без дублювання,
   скасування, заміни, разове заняття і public API без службових полів.
7. Запусти реальний браузерний flow для public schedule та admin login/protected redirect.
   Для зміненого UI застосуй `ui-spacing-review`: 1440, 820, 390 px, keyboard, console,
   document overflow. Не використовуй production-дані або реальні персональні дані в артефактах.
8. Перевір відсутність runtime/import references на вилучені студенти, журнал, signup і `/dashboard`.
9. Перевір diff на секрети, generated artifacts, непотрібні залежності й ненавмисні сторонні зміни.

Запиши звіт у `info/qa/reports/QA-20260902-02.md` за шаблоном. Оновлюй лише QA-результати
VID-029 у `info/tasks.md`, зберігаючи запит та історію. Не виправляй продукт, не коміть і не публікуй.
Поверни `PASS/FAIL/BLOCKED`, точний перевірений стан, виконані/невиконані перевірки,
дефекти з пріоритетом та посилання на звіт. QA-gate відкритий лише за повного актуального `PASS`.
