# QA-звіт: QA-20260903-01 — VID-042

## Контекст і межі

- Тригер, дата й час: прямий запит на QA; 03.09.2026, Europe/Kyiv.
- ID QA-агента, модель, reasoning: `/root/qa_vid042_retry`; GPT-5, успадкована конфігурація (точний рівень reasoning середовище не розкриває).
- Задачі пакета: VID-042.
- Базовий commit і перевірений стан: `4469c15a421cd19b83ba61a8a83e61e2ecbe2ad8` + dirty/untracked scope перед створенням цього звіту.
- Продуктовий fingerprint (SHA-256): `app/globals.css` `8FFC1A6044AC4B19011A34A39604E998BEE67D2EBE1D4C2A244FE63FD69E868F`; `components/public-schedule-explorer.tsx` `5D0D8ECB644FD68F9538671CF700EE61E12FCDF6204ABC0F7DA69ECE0A3AE809`; `components/public-schedule-explorer.module.css` `D90DE7721B20E16C42C0A097557881A1D1635434209F340B9BAA49152E23FC9E`; `info/public-schedule-responsive-layout.md` `C762AC803C671DA64E6DB252DB0A08D2CACE5D0E96A3AD08FF7E6DFCF6AACD97`.
- Перевірені changed/untracked файли: три продуктові файли вище, `info/README.md`, `info/tasks.md`, `info/public-schedule-responsive-layout.md`, `info/qa/handoffs/QA-20260903-01.md`. QA-звіт і QA-статус не входять до fingerprint реалізації.
- Середовище: локальний Next dev на `127.0.0.1:3012`, публічний `/schedule`, тільки read-only запити; DB writes, міграції, commit, push і deployment не виконувалися.
- Виключено: завершені VID-001–VID-041, production і DB integration harness — пакет не змінює дані, схему, API або ролі.

## Класифікація і план

| Задача | Тип, визначений QA | Підстава | Ризик | Сценарії та регресії |
| --- | --- | --- | --- | --- |
| VID-042 | Баг — responsive layout / UX-щільність | Виправляє наявні різні осі header/workspace і зайвий вертикальний розрив, не вводить нову поведінку | P1: overflow, втрата URL/фільтра, focus або touch-area | Diff/CSS review; `tsc`, Vitest, build; `/schedule` на 1440/820/390; navigation, native date, keyboard, console, palette/logo, reduced-motion rules |

## Результати критеріїв

| Задача / критерій | Перевірка або команда | Очікувано | Фактично / доказ | Результат |
| --- | --- | --- | --- | --- |
| Якість коду | `git diff --check` від бази | Немає whitespace-помилок | Пройдено; продуктовий diff обмежений трьома заявленими файлами | PASS |
| Обов'язкові команди | `pnpm exec tsc --noEmit`; `pnpm test`; `pnpm build` | Успіх | exit 0; Vitest: 10 files passed, 1 skipped; 77 tests passed, 9 skipped; Next 16.3.3 build compiled, typecheck і 19/19 pages generated | PASS |
| Desktop 1440 px | Реальний browser, `getBoundingClientRect` / computed style | Header і робоча область на одній осі; один рядок контролів | header і workspace: x=16, width=1392.8; status grid `clock navigation teacher date week`, gap=12, padding=12/14; navigation 181.5×44, teacher 656.5×44, date submit 81.3×44, badge 84.7×32; document 1425/1425 | PASS |
| Tablet 820 px | Реальний browser | Логічні два рядки без overflow | header/workspace: x=12, width=780.8; grid `clock teacher week` / `navigation date date`; controls 44 px; document 805/805 | PASS |
| Mobile 390 px | Реальний browser | Повноширинні control rows; тільки day tabs має горизонтальний scroll | grid `clock week` / `navigation navigation` / `teacher teacher` / `date date`; nav/teacher 355.2×44, date 267.9×44, submit 81.3×44; document 375/375. Tabs `overflow-x:auto`, 468/375 px; real horizontal scroll changed `scrollLeft` 0 → 92.8 while document stayed 375/375 | PASS |
| Навігація й URL | Реальні кліки previous/next, day URL, native date submit; static review `scheduleHref` | Дата і чинний URL-контракт зберігаються | Previous/next дали відповідні `/schedule?date=` та новий заголовок дня; введення `2026-09-05` + «Перейти» дало URL і heading 05.09.2026; diff не змінює `scheduleHref`, query або дані | PASS |
| Фільтр викладача | Static diff + native control browser review | Auto-submit і фільтр не регресують | `onChange → form.requestSubmit()` та hidden `date` перенесені без зміни логіки; native select має доступну назву і 44 px. In-app browser не гідратував client handler (годинник лишався `--:--`), тому повторний автоматичний submit не можна було ізольовано спостерегти; прямий date form submit працює. Це обмеження не зачіпає diff, що не змінює handler. | PASS (non-regression статично) |
| Keyboard і accessibility | Реальний browser на 390 px | Tab/Shift+Tab і focus-visible | Tab: «Розклад» → «Перенесення пар»; Shift+Tab повертає. Outline `rgba(15,118,110,.36) solid 2.4px`. `aria-label` попереднього/наступного дня, label select/date і 44 px контролі наявні | PASS |
| Публічна навігація, палітра, logo, week | Реальний browser + source review | Без регресії бренду, термінів і public nav | `/transfers` відкрився та Back повернув `/schedule`; BrandMark лишив аркуш, галочку, згин і крапку. Computed colors: page `rgb(245,242,235)`, status white, badge teal `rgb(214,241,237)`; «Чисельник» збережено | PASS |
| Console і reduced motion | Browser logs; CSS review | Без errors/warnings; анімації поважають reduce | console error/warn: `[]`; unchanged `@media (prefers-reduced-motion: reduce)` disables schedule transitions/spinner and global transition/animation duration | PASS |
| DB integration | Не запускалося | Не застосовується до CSS/TSX layout-only scope без DB writes | N/A: пакет не змінює DB/access layer; запуск заборонений межами handoff | N/A |

## Потребує виправлень

Підтверджених дефектів немає.

## Неперевірене й обмеження

- In-app browser для локального dev віддав статичну client boundary (`--:--` замість гідратованого годинника), тому неможливо динамічно довести саме React `onChange` auto-submit чи pending-індикатор. Вони не змінені diff; статичний non-regression review виконано. Це не блокує layout-only пакет.
- Emulation `prefers-reduced-motion` не підтримується доступною browser capability; CSS rule перевірено статично. Жодної нової анімації у diff немає.

## Прибирання

- Створений QA dev server на port 3012 та всі його дочірні процеси зупинені.
- Тимчасову in-app browser tab закрито, viewport override скинуто.
- `next-env.d.ts`, змінений `next dev`, повернуто до production type references; QA-артефакти не лишалися.

## Висновок

- Результат пакета: `PASS`.
- Підтверджені задачі: VID-042.
- Задачі, що потребують виправлень: немає.
- Заблоковані критерії: немає; browser limitation зафіксовано вище як non-regression обмеження.
- Актуальність: лише для fingerprint і dirty scope, описаних у цьому звіті; будь-яка зміна реалізації або критеріїв анулює висновок.
- QA-gate для релізу: відкритий для цього exact scope. Це не є дозволом на deployment.

## Додаток: уточнення фінального результату

Цей додаток є append-only корекцією попереднього висновку в цьому файлі. Після додаткової перевірки обов'язкового критерію auto-submit викладача первинний `PASS` **скасовано**.

- `pnpm build` повторено з явним capture exit: `QA_BUILD_EXIT=0`; Next compiled, typecheck і 19/19 routes згенеровано.
- Реальний Playwright CLI не стартував: у host немає Chromium distribution `chrome`. Не встановлював новий browser binary, бо це поза дозволеним UI-only QA scope.
- Альтернативні `edge` та `chrome` browser surfaces недоступні; discovery повернув лише Codex In-app Browser.
- In-app Browser на локальному dev рендерить статичну client boundary: годинник не гідратується й залишається `--:--`; selection native `<select>` змінює DOM, але не запускає React `onChange → requestSubmit`. На відміну від цього, native date form submit справно навігує. Отже dynamic auto-submit/pending неможливо підтвердити у доступному середовищі.

Підтверджених дефектів продукту немає: виявлено обмеження QA-середовища, а не відтворений дефект. Проте auto-submit — обов'язковий interaction criterion, тому:

- Результат пакета: `BLOCKED`.
- QA-gate для релізу: закритий, доки незалежний реальний browser не підтвердить teacher auto-submit і pending behavior на тому самому fingerprint.

## Retest заблокованого критерію — системний Edge

Дата: 03.09.2026. Цей запис append-only і скасовує лише фінальний `BLOCKED` з попереднього додатка; попередню історію збережено.

- Середовище: локальний production server `next start` на `127.0.0.1:3013`; системний `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`, запущений headless через вже наявний `playwright-core`. Нові залежності не додавалися, production DB не змінювалася.
- Fingerprint реалізації повторно звірено: `app/globals.css` `8FFC1A6044AC4B19011A34A39604E998BEE67D2EBE1D4C2A244FE63FD69E868F`; `components/public-schedule-explorer.tsx` `5D0D8ECB644FD68F9538671CF700EE61E12FCDF6204ABC0F7DA69ECE0A3AE809`; `components/public-schedule-explorer.module.css` `D90DE7721B20E16C42C0A097557881A1D1635434209F340B9BAA49152E23FC9E` — без змін після попереднього прогону.
- Hydration підтверджено: clock показав фактичний час `10:30`, не placeholder `--:--`.
- Реальний `selectOption` першого викладача виконав `onChange → requestSubmit`: URL змінився на `?teacher=…&date=2026-09-03`, selected teacher зберігся, а кількість карток занять змінилася з 42 до 2.
- Близька URL-регресія: «Наступний день» дав `?date=2026-09-04&teacher=…`; native date `2026-09-05` + «Перейти» дав `?date=2026-09-05&teacher=…`. Отже обидві дії зберігають фільтр викладача.
- Pending підтверджено в реальному browser: при контрольованій 900 ms затримці переходу «Наступний день» assistive status містив `Завантаження: наступний день…`; після навігації він очищався.
- Console: немає помилок, пов'язаних із `/schedule` або фільтром. Edge зафіксував 404/MIME errors тільки для `/_vercel/insights/script.js`: локальний `next start` не віддає Vercel Analytics asset. Це передіснуюче середовищне обмеження, не змінене diff VID-042, і не блокує ретест interaction criterion.
- Прибирання: Edge закрито самим runner; production server 3013 та всі дочірні процеси зупинено.

Фінальний результат пакета після ретесту: `PASS`. Teacher auto-submit, pending feedback і пов'язане збереження URL підтверджено реальним browser; QA-gate відкритий для описаного fingerprint. Це не є дозволом на deployment.
