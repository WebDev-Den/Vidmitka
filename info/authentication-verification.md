# Перевірка власної автентифікації

Дата: 27 серпня 2026 року.

## Реалізовані сценарії

- реєстрація за ПІБ, email і паролем;
- початкова роль захищеного адміністратора для дозволеної адреси та правильного коду лише до появи адміністратора в БД;
- новий викладач отримує статус очікування;
- адміністратор схвалює викладача в розділі «Викладачі»;
- вхід за email і паролем;
- завершення серверної сесії під час виходу;
- серверна перевірка сесії, ролі та статусу схвалення;
- блокування облікового запису на 15 хвилин після п’яти невдалих входів.

## Результати функціональної перевірки

Початкові результати нижче стосуються переходу на власну автентифікацію. Нові правила початкового коду, призначення ролей і суміщення викладацьких прав перевірені окремо — [administrator-roles.md](administrator-roles.md).

| Сценарій | Результат |
| --- | --- |
| Реєстрація нового викладача | Створено обліковий запис, відкрито сторінку очікування |
| Прямий перехід pending-викладача до приватного розділу | Перенаправлено на сторінку очікування |
| Реєстрація адміністратора без коду | Відхилено без створення привілейованого акаунта |
| Реєстрація адміністратора з правильним кодом | Відкрито адміністративний кабінет без ручного схвалення |
| Схвалення викладача адміністратором | Статус збережено, лічильник заявок оновлено |
| Вхід із неправильним паролем | Показано загальну помилку без створення сесії |
| Вхід схваленим викладачем | Відкрито кабінет із меню викладача |
| Прямий перехід викладача до адміністративного розділу | Доступ відхилено, виконано перенаправлення |
| Перехід у приватний розділ після виходу | Перенаправлено на форму входу |
| П’ять невдалих входів, потім правильний пароль | Вхід тимчасово заблоковано на 15 хвилин |
| Browser console після переходу на власну auth-модель | Нових errors немає |

Перевірку виконано через реальні форми у браузері. Створено лише два тимчасові тестові акаунти; після перевірки вони й пов’язані сесії видалені. Реальний обліковий запис власника не створювався.

## Автоматичні перевірки

- `pnpm test` — 26 тестів пройшли.
- `pnpm exec tsc --noEmit` — пройдено.
- `pnpm build` — пройдено.
- `pnpm db:migrate` — міграцію власних акаунтів і сесій застосовано.
- `pnpm db:verify-import` — пакетний імпорт і конфлікти працюють після переходу на внутрішні user ID.
- `pnpm db:verify-semester` — завершення семестру зберігає студентів і їхні предметні зв’язки.

## Обмеження

- Відновлення та зміна пароля — наступний окремий етап.
- Email не підтверджується листом: викладача перевіряє адміністратор, а першу реєстрацію адміністратора захищає серверний код.
- Дизайн та відступи в цій зміні не перевірялися за окремою вказівкою користувача.

## Доповнення: валідація пароля, 27.08.2026

Межа перевірки — погоджений сценарій реєстрації: форма → серверна дія → створення акаунта → повторний вхід. Чинні облікові записи та їхні паролі не змінюються; міграція БД для цього доповнення не потрібна.

- [x] Правила на клієнті та сервері: 6–128 символів, велика літера, цифра й спецсимвол; точний збіг обов’язкового підтвердження.
- [x] 14 доданих автоматичних перевірок: гранична довжина, кожна вимога складності, українські великі літери, відсутнє/порожнє/відмінне підтвердження, збереження введених пробілів і сумісність старих паролів під час входу.
- [x] `pnpm test` — 126 пройдено; 3 інтеграційні тести в звичайному запуску пропущено, оскільки вони потребують ізольованої БД.
- [x] `node --env-file=.env.local info/testing/attendance-runner.mjs --administrators --browser` — інтеграційний сценарій пройдено. Прямі виклики серверної дії без клієнтської перевірки відхиляють недійсні паролі та підтвердження; акаунт не створюється.
- [x] Браузер: окремі повідомлення про відсутність великої літери, цифри й спецсимволу; повідомлення «Паролі не збігаються.»; відхилення 5-символьного пароля; реєстрація із 6-символьним паролем та успішний повторний вхід.
- [x] `pnpm exec tsc --noEmit` і `pnpm build` — успішні. Перший запуск збірки зупинився на Windows `spawn EINVAL`; повторний запуск тієї самої команди пройшов без змін застосунку.
- [x] Після оновлення правил AGENTS.md виконано `ui-spacing-review` тільки форми реєстрації на 1440, 820 і 390 px: немає горизонтального переповнення, поля й помилки вміщуються. Вимірювання — у [ui-spacing-review.md](ui-spacing-review.md). CSS, палітру та логотип не змінено.
- [x] Нових помилок або попереджень у консолі браузера немає. Тестові акаунти, сесії, схеми й ролі видалено разом з ізольованим середовищем; робочі дані не змінено.

Автоматичне перенесення фокусу на помилкове поле підтверджено. Окрема перевірка фізичних Tab/Enter залишилася непідтвердженою: у керованому браузері надсилання цих клавіш не змінювало фокус або стан форми; це не записано як успішний тест. Надсилання кнопкою й відображення помилок перевірено.

Зміни коду залишаються локальними; Git push і production deployment цього доповнення не виконувалися.
