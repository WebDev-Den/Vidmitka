# Доручення незалежному QA: VID-048–VID-050

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`, `.agents/skills/qa-batch/SKILL.md`, `info/qa/process.md`, `info/qa/report-template.md`, `info/tasks.md` (VID-048–VID-050), `info/public-teacher-filter.md` і `info/public-schedule-responsive-layout.md`. Ти вже QA-агент: не делегуй цей QA іншому агенту.

## Межі

- Тригер: прямий запит користувача «можеш видалити і перенести перевірити і задеплоїти».
- Пакет: `QA-20260904-02`.
- Задачі: VID-048, VID-049; VID-050 — технічний QA/release gate.
- Первинні запити:
  - VID-048: «не деплой подивись по стилям»; скриншот показує надмірно широкий/високий popup викладачів і порожній верхній блок.
  - VID-049/050: «можеш видалити і перенести перевірити і задеплоїти»; скриншот перекреслює верхні пункти `/schedule`, виділяє окрему панель контролів і стрілкою вказує перенесення у вільний простір header.
- База: `ff908f932916a14487cd2f8fb1fec1ad944d1b97`.
- Перевірюваний стан: вказаний `HEAD` + dirty tracked-файли `app/(public)/schedule/page.tsx`, `app/globals.css`, `components/public-header.tsx`, `components/public-schedule-explorer.module.css`, `components/public-schedule-explorer.tsx`, `info/README.md`, `info/public-schedule-responsive-layout.md`, `info/public-teacher-filter.md`, `info/tasks.md` і цей handoff. Самостійно зафіксуй повний перелік та SHA-256 продуктового обсягу; QA-документи не включай у продуктовий fingerprint.
- Виключення: старі вже опубліковані задачі; production-БД; будь-які нові продуктові зміни поза VID-048–VID-049.
- Середовище: локальний застосунок і чинний read-only публічний розклад. Не записувати production-дані та не публікувати значення env.
- Режим: лише QA; продукт не редагувати, commit/push/deploy/production migration заборонені. Дозволені записи: QA-звіт і лише QA-поля VID-048–VID-050 у `info/tasks.md`.

## Критерії та ризики

Самостійно класифікуй запити й склади один ризик-орієнтований план. Не довіряй статусу «реалізовано».

1. Запусти обов'язкові `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build`; зафіксуй exit codes, кількість тестів і пропуски. DB integration познач `N/A` лише якщо обґрунтуєш, що пакет змінює виключно компонування/стилі без data/repository меж.
2. Для браузерного UI повністю прочитай потрібний browser/Playwright skill та `.agents/skills/ui-spacing-review/SKILL.md`. Перевір `/schedule` на 1920, 1440, 820 і 390 px:
   - канонічний логотип, toolbar і вкладки днів утворюють один header;
   - немає дубльованих верхніх пунктів «Розклад / Перенесення пар» та окремої bordered status-card;
   - desktop використовує вільну ширину; tablet/mobile коректно складаються;
   - розклад починається одразу після header без надмірної порожнечі;
   - немає document-level horizontal overflow, перекриття або обрізаних основних контролів; вкладки днів прокручуються лише у власному контейнері на mobile;
   - виміряй фактичні padding/gap/ширини та внеси їх до звіту.
3. Перевір popup пошуку викладачів на desktop і mobile: ширина не більша 520 px та viewport, список не вищий 252 px, рядки не нижчі 36 px, немає порожнього верхнього блоку; partial/no-match, scroll, pointer, Arrow Up/Down, Enter, Escape, focus і auto-navigation працюють.
4. Перевір навігацію попередній день / сьогодні / наступний день, вкладки днів і date form; teacher query зберігається. Pending-стани не зависають.
5. Відкрий `/transfers`: звичайний header і перехід `/schedule` мають лишитися доступними; сторінка не повинна мати нових layout-регресій.
6. Перевір browser console без нових error/warning, `focus-visible`, 44 px основні touch targets і `prefers-reduced-motion` у межах змінених елементів.

Запиши звіт у `info/qa/reports/QA-20260904-02.md` за шаблоном. Оновлюй лише QA-результати VID-048–VID-050, зберігаючи первинні запити й історію. Зупинись після завершеного прогону та звіту; дефекти поверни основному агенту. Поверни `PASS` / `FAIL` / `BLOCKED`, перевірений fingerprint, що виконано, що потребує виправлень, що не перевірено та посилання на звіт. QA-gate не є дозволом на deployment, але користувач уже окремо дозволив deployment після `PASS`.
