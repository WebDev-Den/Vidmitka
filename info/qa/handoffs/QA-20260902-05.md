# Доручення незалежному QA: QA-20260902-05

## Роль і тригер

Твоя роль — незалежний QA-агент Vidmitka. Прочитай повністю `AGENTS.md`, `.agents/skills/qa-batch/SKILL.md`, `info/qa/process.md`, `.agents/skills/ui-spacing-review/SKILL.md`, інструкції доступного browser/Playwright skill і задачі VID-033–VID-037 в `info/tasks.md`. Ти вже QA-агент: не делегуй цей QA іншому агенту.

Тригер: прямий запит користувача «перевір чи все реалізовано і залий» перед дозволеним push у `main` та production deployment.

## Пакет і базова межа

- Пакет: VID-033, VID-034, VID-035, VID-036; VID-037 — релізна операція після PASS.
- База: поточний `HEAD` / `origin/main` до нового commit. Перевір повний dirty worktree, включно з усіма untracked файлами.
- Зміни продукту охоплюють мінімальну адмін-оболонку, календар перенесень і публічну таблицю, server-side calendar resolver, загальний розклад без group picker із групами на картці та фільтр конкретного / усіх викладачів.
- Самостійно зафіксуй точний перелік файлів і SHA-256 відбиток перевіреного dirty scope. QA-звіт і оновлення QA-полів не включай у відбиток продукту.

## Критерії й ризики

1. Самостійно класифікуй задачі та зістав кожен критерій VID-033–VID-036 з реалізацією; не виходь за їхній обсяг.
2. Перевір мінімальну адмінку: усі маршрути й дії доступні, згортання desktop sidebar і mobile dialog доступні з клавіатури, поточний маршрут зрозумілий, немає втрати admin-функцій.
3. Перевір календар перенесень: pure-правила, точний набір 12 дат, CRUD/batch/context/template resolver в ізольованій БД, публічну `/transfers`, звичайний день і день перенесення. Не записуй production-дані.
4. Перевір публічний `/schedule`: групового picker немає; групи показуються при hover/focus і постійно на touch/mobile; паралельні заняття не втрачаються.
5. Перевір filter викладача: «Всі викладачі», конкретний активний викладач, викладач без занять, невалідний / застарілий ID; день/тиждень, tabs, стрілки, «Сьогодні», date form і Back/Forward зберігають `teacher`; фактичні викладачі винятків мають пріоритет над базовими.
6. Виконай щонайменше `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build` і релевантний ізольований `pnpm test:schedule-v2-integration`. Пропущені DB-тести у звичайному unit suite не вважай пройденими.
7. Для UI використай реальний браузер і `ui-spacing-review`: 1440, 820, 390 px; основні flow, клавіатура, focus, console без нових errors/warnings, touch targets і відсутність document-level overflow. Артефакти збережи у `info/qa/artifacts/QA-20260902-05/` без реальних персональних даних.
8. Перевір diff/secrets/generated files. Не коміть, не пуш, не деплой і не змінюй production БД, домени, env або ролі.

## Дозволи й результат

- Можна запускати локальні команди, dev server і створювати/прибирати лише власну ізольовану QA-схему/процеси.
- Не виправляй product-код. Можна додати лише справді необхідний тест або QA-артефакт і пояснити це у звіті.
- Створи `info/qa/reports/QA-20260902-05.md` за шаблоном і онови QA-поля VID-033–VID-037 у `info/tasks.md`.
- Поверни однозначний `PASS`, `FAIL` або `BLOCKED`; переліч знайдені дефекти з пріоритетом і вкажи, чи дозволяє висновок реліз.
