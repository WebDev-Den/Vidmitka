# QA-20260828-01 — незалежна передрелізна перевірка

Тригер: прямий запит «перевір залий на гід задеплой щоб все працювало» (VID-009).

База: `58a3f9151b56041f0e4feebf558ce7a160fe6c4f`, гілка `main`, remote `https://github.com/WebDev-Den/Vidmitka.git`. Обсяг — увесь поточний diff від бази, включно з untracked-кодом, SQL, тестами й документами VID-001–VID-008; секрети, `.next`, `.vercel`, build/browser-артефакти виключені. Зафіксувати фактичний SHA й контрольні суми коду самостійно, не довіряти лише цій назві бази.

Первинні запити й критерії — відповідні розділи [tasks.md](../../tasks.md). Статус «реалізовано» не є доказом. Самостійно класифікувати запити й оцінити ризики:

- VID-001: захищений початковий адміністратор, закриття bootstrap-реєстрації, призначення ролей і викладацькі права адміністратора.
- VID-002: складність пароля й підтвердження, клієнтські/серверні негативні випадки, незмінний вхід.
- VID-003: календар відпрацювань, спільний контекст дати, права, версії, конкуренція зі збереженням журналу й історичні знімки.
- VID-004: аудиторії та валідність експорту Server Actions у пов’язаних модулях.
- VID-005: журнал, дозволи на реалізацію/TDD та незалежний пакетний QA.
- VID-006: п’ять реальних найближчих занять, типи, імпорт, права зміни власного/призначеного заняття, історія журналу.
- VID-007: шкала пар/перерв, київський час, гідратація/таймер, контраст і адміністраторські кольори.
- VID-008: поточний домен лише `vidmitka.vercel.app`; зовнішню прив’язку можна перевірити тільки read-only.

Модель QA: `gpt-5.6-sol`, reasoning `high` (ролі, міграції, збереження історії). QA не створює наступних QA-агентів. Основний агент не змінює код/тести під час прогону й не дублює перевірки.

Прочитати повністю AGENTS.md, qa-batch, [process.md](../process.md), [report-template.md](../report-template.md), необхідні browser/testing skills і ui-spacing-review. Виконати один повний ризик-орієнтований прогін, не зупинятися лише на першому дефекті, якщо решта перевірок безпечно доступна.

Мінімум: typecheck, unit, production build; усі релевантні ізольовані integration-сценарії через `info/testing/attendance-runner.mjs` (журнал, groups, administrators, rooms, calendar, upcoming, periods). Пропуски DB-тестів у звичайному `pnpm test` не означають PASS. Міграції 009–011 перевірити на ізольованих даних, у тому числі повторний запуск/старі записи, захист журналів і попередніх налаштувань. Дозволено додати необхідні тести/QA-скрипти лише в `info/testing/` чи релевантних тестових файлах — не змінювати продукт.

Локальні env ведуть до робочої БД. НЕ запускати робочий `pnpm db:migrate`, не змінювати реальні дані. Використовувати наявний harness з порожньою тимчасовою схемою та окремою роллю без прав запису до public; перед запуском прочитати harness. Не передавати секрети в логах чи артефактах. Міграція 008 уже застосована до робочої БД — не повторювати її там. Production і домени тільки read-only, без реєстрацій/зміни ролей чи журналів.

Для UI: реальний браузер в ізольованому середовищі, 1440/820/390 px, keyboard, console, overflow, чинний логотип/палітра, фактичні вимірювання ui-spacing-review. Власні артефакти — `info/qa/artifacts/QA-20260828-01/`; не створювати реальні студентські дані. Якщо браузерний runtime недоступний, зафіксувати конкретну причину BLOCKED, не підміняти кодовими припущеннями.

Повернути PASS/FAIL/BLOCKED, докази, дефекти з пріоритетами та що не перевірено. Звіт — `info/qa/reports/QA-20260828-01.md`, QA-поля задач — `info/tasks.md`. Не комітити, не пушити, не деплоїти. Релізну операцію VID-009 у цілому не позначати завершеною до фактичної публікації основним агентом; висновок QA стосується перевіреного пакета VID-001–VID-008.

## Повторний прогін після QA-01/QA-02

Перший FAIL зберігається у звіті. Після завершення первинного прогону основний агент одним пакетом уточнив тип клієнта Neon до фактичних стандартних options (`false, false`), актуалізував очікування інвалідації головної в schedule-week test і виключив QA artifacts із Git/Vercel upload. Іншого коду не змінював; сам тестів не запускав.

Повторно перевірити tsc/unit/build і вплив типізації клієнта на всі сім DB-сценаріїв. Далі завершити весь відкладений браузерний пакет у production-mode ізольованого harness (з `tty: true` для штатного `stop`), включно з основними/негативними/рольовими сценаріями й ui-spacing-review 1440/820/390. Production лише read-only. Новий fingerprint має відповідати виправленому коду; додати результати до того самого звіту, не стирати первинні дефекти. Після запуску знову діє freeze продукту до завершення QA.

## Повторний прогін після QA-03/QA-04

Після завершення повного browser batch і явного зняття freeze основний агент виправив два підтверджені P2 разом: вертикальне прокручування sidebar без стискання її вмісту; фокус мобільного меню, Tab/Shift+Tab, Escape/повернення фокусу, inert для фонового робочого простору, блокування прокручування фону та cleanup при переході до desktop. Змінені тільки `app/globals.css` і `components/private-shell.tsx`; логотип, палітра, доменні правила й доступ до даних незмінні. Самоперевірки не запускалися.

Перевірити tsc/unit/production build і production-browser регресії меню на 390×740, 390×844, 820 px та 1440 px (включно зі згорнутим desktop). Підтвердити досяжність «Пари та час» і «Налаштування», нормальні висоти контролів, scroll/overflow, цикл фокусу в обох напрямках, close/backdrop/Escape/навігацію, повернення фокусу й відсутність inert/scroll-lock після закриття або зміни breakpoint. QA сам визначає необхідний повтор уже успішних незмінених доменних сценаріїв. Підсумок і новий fingerprint — у тому самому звіті; до нього push/deploy лишаються заборонені.
