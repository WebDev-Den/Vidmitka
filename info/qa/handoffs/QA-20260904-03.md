# Доручення QA-20260904-03

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`, `.agents/skills/qa-batch/SKILL.md`, `.agents/skills/ui-spacing-review/SKILL.md`, `info/qa/process.md`, `info/tasks.md` (VID-051) і потрібні browser/Playwright skill-інструкції. Ти вже QA-агент: не делегуй цей QA іншому агенту.

- Тригер: користувач підтвердив «виконуй» щодо реалізації fallback, кнопки повернення та перевірки responsive/UX.
- Пакет: `QA-20260904-03`.
- Задача: `VID-051`.
- Первинні запити:
  1. «можеш прибрати schedule і щоб url не змінюався і зберігалось в кукі або локальн стор вибраний викладач а день автоматично при онові вибирався день»;
  2. «це зафіксуй а низ має скролитись і при завантажені має скролитись до поточної пари»;
  3. після пропозиції fallback для низьких екранів / 200%, кнопки «До поточної пари» та QA — «виконуй».
- База: `ab5e964` (останній опублікований Git baseline перед dirty package).
- Перевірюваний стан: `HEAD ab5e964` плюс усі tracked і untracked зміни робочого дерева для VID-051. Самостійно зафіксуй перелік і SHA-256 продуктового обсягу без env/секретів.
- Виключення: історичні QA-артефакти й завершені VID-001–VID-050; production DB writes, push, commit і deployment.
- Середовище: чинний локальний Next/Vitest/Playwright harness. Не змінюй production-дані; DB integration — лише якщо знайдеш релевантний ізольований сценарій, без секретів у звіті.
- Режим: лише QA. Продукт не редагувати. Дозволені записи: звіт, артефакти QA, QA-поле VID-051 і необхідні тести лише якщо бракує погодженого публічного seam.

Самостійно класифікуй VID-051, склади ризик-орієнтований план і зістав кожен критерій із фактичним результатом. Обов'язково:

- `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build`;
- перевірити `/`, redirect `/schedule?date=...&teacher=...` → чистий `/`, відсутність зміни URL/history при всіх контролах;
- cookie викладача: вибір, reload, «Всі викладачі», невалідне/неактивне значення; reload завжди показує сьогодні Europe/Kyiv;
- day API без групи, фільтр teacher, негативні UUID/date та відсутність admin fields;
- pending/error/race настільки, наскільки безпечно відтворити локально;
- реальний браузер на 1440, 820 і 390 px: fixed header + day heading, внутрішній вертикальний scroll, keyboard focus, відсутність document-level horizontal overflow, popup викладача, 44 px controls;
- low-height viewport до 680 px і типовий 200% zoom: header/розклад не обрізані, document scroll fallback працює;
- початкове позиціонування сьогодні на current/next/last period відповідно до часу, відсутність автоскролу іншої дати й повторного стрибка від clock update;
- після ручного віддалення від цільового рядка кнопка «До поточної пари» з'являється, повертає рядок у viewport, зникає і працює клавіатурою; reduced motion;
- `/transfers` передає дату на canonical `/` одноразово без query, наступний reload повертає сьогодні.

Запиши звіт у `info/qa/reports/QA-20260904-03.md` за шаблоном. Онови лише QA-результат VID-051, зберігаючи первинні запити та історію. Зупинись після прогону; дефекти поверни основному агенту. У висновку дай `PASS`, `FAIL` або `BLOCKED`, перевірений fingerprint, виконане, неперевірене й стан QA-gate. Успішний QA не є дозволом на push/deployment.
