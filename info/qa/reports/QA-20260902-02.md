# QA-звіт: QA-20260902-02 — нова концепція розкладу VID-029

## Контекст і межі

- Тригер, дата й час: прямий запит користувача «перевір чи все працює і задеплой»; 02.09.2026, 14:45–15:05 EEST.
- ID QA-агента, модель, reasoning; причина вибору/заміни: `/root/qa_vid_029`, `gpt-5.6-sol`, `high`; модель обрана основним агентом за політикою для auth, міграцій і складної міжмодульної регресії.
- Задачі пакета: `VID-029`. Наявний у журналі `VID-030` не отримує QA-висновку цим звітом; його зміна `components/public-header.tsx` фізично присутня у dirty worktree, тому потрапила до спільних build/browser перевірок, але критерії VID-030 не оцінювалися як окрема задача.
- Базовий commit і перевірений commit / dirty worktree: база й `HEAD` — `5a632ad851f1505b3cc0965d4fe529a1f0d0bf94`; перевірено `HEAD` плюс dirty/untracked стан, переданий у `info/qa/handoffs/QA-20260902-02.md`.
- Файли та відбиток перевіреного обсягу (без секретів): 247 продуктових/документаційних змінених, видалених або untracked файлів; повний manifest — `info/qa/artifacts/QA-20260902-02/verified-files.tsv`, SHA-256 manifest `4e18f65ec79771715265151ce8c358ec144db60dba809a48a6b3bb331441fdde`. З manifest виключено QA-звіт, QA-артефакти й `info/tasks.md`, який змінюється самим QA.
- Середовище й підтвердження ізоляції: `.env.local` містить тільки віддалені Neon endpoints для БД `neondb`; локальних `docker`, `podman` або `psql` немає. Ізольовану тимчасову схему/БД та окрему роль не надано. Жодної DB-write команди, міграції, імпорту або створення адміністратора не виконано. Значення секретів не читалися й не записувалися у докази.
- Що виключено та чому: push, deployment, production migration, робочі дані та env-секрети — заборонені дорученням. Міграція 014, DB preview/commit, повторний імпорт, rollback і CRUD у БД — `BLOCKED`, бо доведено ізольованої БД немає. Успішний admin login не виконувався з тієї ж причини.

## Класифікація і план

| Задача | Тип, визначений QA | Підстава | Ризик | Сценарії та регресії |
| --- | --- | --- | --- | --- |
| VID-029 | Велика фіча / архітектурна зміна | Нові модель v2, auth-межа, імпорт, admin/public UI та вилучення legacy-системи | Високий: auth, транзакційність, міграція, повторний імпорт, формування винятків, великий destructive diff | Обов’язкові команди; pure parser на 431 записі; auth і legacy routes; статична перевірка server actions/API; CRUD/import/migration у безпечній БД; public/admin browser flow; UI 1440/820/390; secret/dependency/generated scan |

План сформовано до завершення прогону. Позитивний статус реалізації в журналі не використовувався як доказ.

## Результати критеріїв

| Задача / критерій | Перевірка або команда | Очікувано | Фактично / доказ | PASS / FAIL / BLOCKED / NOT_RUN / N/A |
| --- | --- | --- | --- | --- |
| VID-029 / TypeScript | `pnpm exec tsc --noEmit` | exit 0 | Початково exit 1 через stale `.next/dev/types`, що посилалися на видалені routes. Після ізоляції generated cache повтори exit 0; після додавання QA-тесту фінальний повтор exit 0. Продуктовий код не змінювався. | PASS |
| VID-029 / unit suite | `pnpm test` | exit 0, усі тести виконані | exit 1; 8 файлів, 72 тести: 71 PASS, 1 FAIL. `lib/schedule-import-v2/parser.test.ts:110`: очікувались `teacher_conflict`, `room_conflict`, `group_conflict`, фактично `duplicate_row`. | FAIL |
| VID-029 / production build | `pnpm build` | exit 0 | Початково exit 1 на тому самому stale `.next/dev/types`; clean повтор exit 0, 19 routes, compile/typecheck/static generation завершені. | PASS |
| VID-029 / parser фактичного JSON | `pnpm exec vitest run info/qa/artifacts/QA-20260902-02/actual-import.test.ts --reporter=verbose` | 431 валідний запис, стабільні унікальні ключі | exit 0; 1/1 PASS. 431 total/valid, 0 invalid, 0 duplicates, 34 викладачі, 53 дисципліни, 17 аудиторій, 159 груп, 3 типи, 56 warnings; повторний pure analysis дав ті самі `sourceId`. | PASS |
| VID-029 / preview конфліктів | Загальний Vitest + код parser | Конфлікти та дублікати мають узгоджений preview-контракт | Репозиторний тест контракту падає. Тестовий fixture має той самий identity key, тому parser класифікує другий рядок як duplicate до collision analysis. Release suite не зелена. | FAIL |
| VID-029 / міграція 014 clean + repeat | Виявлення DB runtimes, аналіз env і SQL | Чиста ізольована схема, перше й повторне застосування без робочих даних | Доступний лише віддалений `neondb`; локального PostgreSQL/Docker немає, окрему QA-схему/роль не доведено. Міграцію не запускали. | BLOCKED |
| VID-029 / DB preview, idempotency, rollback | Integration commit на ізольованій БД | create/update/skip, другий імпорт без дублювання, rollback без часткових записів | DB integration harness для v2 відсутній; безпечне write-середовище відсутнє. Статично `commitTeacherScheduleImport` використовує serializable transaction, але це не замінює виконання. | BLOCKED |
| VID-029 / auth role seam | `lib/auth/authorization.test.ts`, `lib/navigation/role-navigation.test.ts`, статичний огляд | Лише approved administrator; teacher без меню/доступу | Відповідні pure тести PASS у загальному прогоні; `teacher` явно відхиляється. Всі admin server actions викликають `requireAdminPanelUser()`. | PASS |
| VID-029 / protected admin pages | Production server + HTTP/browser | `/admin` і всі `/admin/**` redirect на `/admin/login` без сесії | 11/11 наявних admin routes повернули 307 на `/admin/login`; browser підтвердив доступну форму. | PASS |
| VID-029 / login security | Static auth review + browser | generic errors, throttle, secure session/logout | Generic invalid-credentials message, DB throttle, hashed random token, `HttpOnly`, production `Secure`, `SameSite=Lax`, `__Host-` cookie та revoke/logout наявні. Фактичний wrong/correct login і throttle не виконані без ізольованої БД. | BLOCKED |
| VID-029 / public/legacy routes | HTTP + source scan | Немає public login/signup; legacy routes відсутні | `/dashboard`, `/sign-in`, `/sign-up`, `/approval-pending` — 404. Public navigation містить лише `/schedule` і `/transfers`; видимих login/admin links немає. | PASS |
| VID-029 / відсутність runtime legacy refs | `rg` поза `.next`, `node_modules`, `info` | Немає runtime/import refs на `/dashboard`, signup, студентів і журнал | Студентські/журнальні/signup imports не знайдені, але активний `components/private-shell.tsx:63,180` досі містить спеціальну обробку `"/dashboard"`. | FAIL |
| VID-029 / CRUD довідників і розкладу | Static code review + required DB/browser flow | Повний життєвий цикл, validation, conflicts, dependencies | Server modules/actions/UI для каталогів, periods, entries, exceptions наявні; auth guards і FK error messages читаються статично. Реальні create/update/deactivate/delete, конфлікти й dependency guards не виконані без ізольованої БД. | BLOCKED |
| VID-029 / schedule exceptions | Pure occurrence tests + static SQL | move без дублювання, cancel, replacement, one-time | 3/3 pure occurrence tests PASS. DB queries мають base/moved/one-time branches, але реальні move/reschedule/cancel/replacement/one-time scenarios не виконані. | BLOCKED |
| VID-029 / public API validation/error surface | HTTP | 400 для invalid input, generic server errors, без admin fields | Invalid UUID — 400 з `{error:{code,message}}`; valid UUID у немігрованому env — 503 з generic `SCHEDULE_UNAVAILABLE`, SQL details не витікають у response. Success payload не отримано. | BLOCKED |
| VID-029 / browser public schedule | In-app browser, production build server | День/тиждень, група, empty state, exceptions | `/` redirect на `/schedule`, але сторінка не рендериться: `relation "academic_groups" does not exist` у підключеній віддаленій БД. Production migration заборонена. | BLOCKED |
| VID-029 / UI spacing login | `ui-spacing-review`, browser measurements | 1440/820/390, keyboard, console, no document overflow | Login: 1440 client/scroll 1440/1440; 820 — 805/805; 390 — 375/375 (scrollbar виключено з client width). Inputs 48 px, submit 44 px; focus order brand → email → password → submit; нових console errors немає. | PASS |
| VID-029 / UI spacing public/admin app | `ui-spacing-review` | 1440/820/390, console, overflow, main flows | Protected app потребує login/DB; public schedule падає до UI через відсутню migration 014. Повну spacing/keyboard перевірку цих сторінок не завершено. | BLOCKED |
| VID-029 / automated acceptance coverage | `rg` test suites/cases | Автотести auth route/API, CRUD, week/date schedule, exceptions, import idempotency/rollback, public schedule | Є pure-тести parser, role seam, week rules і 3 occurrence cases. Немає v2 DB integration tests для CRUD/import/public schedule, route/server-action auth tests, replacement/one-time DB tests чи rollback scenario. | FAIL |
| VID-029 / diff hygiene | `git diff --stat`, dependency/secret/generated scan, manifest | Без секретів, випадкових generated файлів і зайвих dependencies | Lockfile та dependencies не змінені; package scripts лише замінено на `admin:init`. Нових secret-like values у tracked additions/untracked scope не знайдено; збіги були лише у видалених legacy tests. `.next` і QA artifacts ignored. | PASS |

## Потребує виправлень

### QA-029-01 · VID-029 · P1 — обов’язковий тестовий прогін падає

- Кроки: у перевіреному dirty state виконати `pnpm test`.
- Очікувано: exit 0; preview test узгоджений зі стратегією identity/deduplication.
- Фактично: exit 1, 1/72 FAIL у `lib/schedule-import-v2/parser.test.ts:110`; fixture із тим самим teacher/date/period/groups отримує `duplicate_row` до collision warnings.
- Доказ: фактичний Vitest output у цьому звіті; 71 PASS, 1 FAIL.
- Вплив: обов’язковий test gate не пройдений; реліз заборонений. Потрібно узгодити тестовий fixture/очікування зі стабільним import key, не послаблюючи реальний collision preview.

### QA-029-02 · VID-029 · P1 — відсутнє обов’язкове DB/integration покриття v2

- Кроки: перелічити `*.test.ts` та зіставити з розділом 14 первинної специфікації.
- Очікувано: автоматизовані сценарії захисту admin routes/actions, CRUD, public schedule, move/cancel/replacement/one-time, import idempotency та rollback.
- Фактично: вісім repository test files покривають переважно pure rules/parser; v2 DB integration harness і названі сценарії відсутні.
- Доказ: `rg '^describe\(' --glob '*.test.ts'` та `rg '^\s*(it|test)\('`; у suite немає DB CRUD/import/public/auth-action scenarios.
- Вплив: ключові acceptance paths не мають відтворюваної перевірки, а в поточному середовищі залишаються `BLOCKED`; релізний висновок не може бути `PASS`.

### QA-029-03 · VID-029 · P3 — залишився runtime literal legacy `/dashboard`

- Кроки: `rg -n '/dashboard' components app lib`.
- Очікувано: після вилучення legacy dashboard немає runtime/import references.
- Фактично: `components/private-shell.tsx:63` і `:180` містять `"/dashboard"` у логіці визначення активної навігації.
- Доказ: source scan; сам route `/dashboard` правильно повертає 404.
- Вплив: функціонального доступу до legacy route немає, але критерій повного вилучення/dead-code cleanup порушений.

## Неперевірене й обмеження

- Міграція 014 на чистій схемі та повторне застосування.
- DB preview/commit фактичного JSON, create/update/skip counts, idempotency і rollback критичної помилки.
- Фактичний correct/wrong admin login, throttle, session cookie issuance та logout.
- CRUD довідників, базового розкладу й винятків; конфлікти та FK/dependency behavior.
- Успішний public API payload без службових полів.
- Public schedule day/week, перенесення без дублювання, cancellation, replacements, one-time lesson і повний UI spacing на 1440/820/390.

Причина всіх DB/browser обмежень одна: доступний env вказує на віддалений `neondb`, де v2-таблиці не застосовано; без окремої тимчасової схеми/ролі неможливо довести ізоляцію. Тайм-аутів не було; заблоковані перевірки не зараховані як успішні.

## Прибирання

- Локальний production server на порту 3012 зупинено; створені browser tabs закрито; viewport override скинуто.
- Початковий stale `.next/dev/types` був відокремлений для clean run; clean `next build` сам перегенерував `.next` і прибрав stale generated cache. Продуктові файли не змінювались.
- DB schema/roles/rows не створювалися й не видалялися.
- Залишено дозволені артефакти: `actual-import.test.ts` і `verified-files.tsv` у `info/qa/artifacts/QA-20260902-02/`.

## Висновок

- Результат пакета: `FAIL`.
- Підтверджені задачі: немає повністю підтвердженої задачі; окремі pure/auth/build критерії VID-029 пройшли.
- Задачі, що потребують виправлень: `VID-029` — QA-029-01, QA-029-02, QA-029-03.
- Заблоковані або неперевірені критерії: migration 014; DB CRUD/import/idempotency/rollback; successful login/session/logout; public schedule/API success; повний browser/UI flow.
- Актуальність для конкретного стану коду: тільки для `HEAD 5a632ad851f1505b3cc0965d4fe529a1f0d0bf94` + dirty/untracked manifest SHA-256 `4e18f65ec79771715265151ce8c358ec144db60dba809a48a6b3bb331441fdde`. Зміна продуктового файлу, тесту або критерію робить висновок неактуальним.
- QA-gate для релізу: **закритий** — є підтверджені FAIL і незавершені обов’язкові DB/browser checks. Push/deployment не дозволені цим звітом.

Успішний повторний QA після пакета виправлень потребує доведено ізольованої PostgreSQL schema/database з окремою роллю, щоб виконати migration/import/CRUD/browser сценарії без production-даних.

---

## Повторний прогін після виправлень — 02.09.2026

### Контекст повторного прогону

- Час: 15:17–15:23 EEST.
- QA-агент: той самий `/root/qa_vid_029`, `gpt-5.6-sol`, reasoning `high`.
- Перевірюваний стан: `HEAD 5a632ad851f1505b3cc0965d4fe529a1f0d0bf94` плюс оновлений dirty/untracked scope.
- Новий manifest: 251 продуктовий/документаційний файл у `info/qa/artifacts/QA-20260902-02/verified-files-rerun.tsv`; SHA-256 `650b7723a1112d7ad61ff5e40c3fc37b24bad218daef46b46cd086258d74fc95`. Як і в першому прогоні, QA report/artifacts та QA-mutated `info/tasks.md` не входять до продуктового відбитка.
- Зміни, заявлені для повтору: parser conflict fixture; вилучення runtime `/dashboard`; нові isolated DB helper, integration test, runner, browser wrapper, package script і документація.

### Safety preflight isolated harness

До першого write-запуску статично підтверджено:

- випадкове ім’я `qa_vid029_${randomBytes(8).toString("hex")}`;
- cleanup приймає лише `/^qa_vid029_[a-f0-9]{16}$/`;
- `current_schema()` перевіряється до створення prerequisites або migration;
- помилка setup викликає `DROP SCHEMA IF EXISTS <random-name> CASCADE`;
- у product/harness SQL немає явних `public.` write references; `public.` використано лише в read-only QA fingerprint command.

Read-only baseline перед harness:

- `current_schema=public`;
- `pgcrypto=false`;
- public base tables: 18;
- SHA-256 точних row counts усіх public tables: `eb59d249fef1a49d386be3f980e1abbc0745176a53357cfcdbe061c29cfccccb`;
- наявних `qa_vid029_*` schemas: 0.

Harness спробував створити випадкову схему, але scoped connection не змінив `search_path`: `current_schema()` повернув `public`. Guard зупинив setup до prerequisites/migration. Після автоматичного cleanup read-only fingerprint повторено: `pgcrypto=false`, 18 public tables, той самий SHA-256 `eb59d249fef1a49d386be3f980e1abbc0745176a53357cfcdbe061c29cfccccb`, QA schemas: 0. Отже, public/production tables і extension state не змінені.

### Статус попередніх дефектів

| Дефект | Повторна перевірка | Статус |
| --- | --- | --- |
| QA-029-01 — parser preview test | `pnpm test`: fixture тепер дає очікувані collision warnings; загальний suite 73 PASS | **RESOLVED** |
| QA-029-02 — немає v2 integration coverage | Test/harness files додані, але integration suite не виконав 6 сценаріїв через несправну schema scoping; coverage ще не дає фактичного доказу | **PARTIALLY ADDRESSED / OPEN** |
| QA-029-03 — runtime `/dashboard` | `rg` поза `.next`, `node_modules`, `info` не знайшов `/dashboard`, signup, journal, attendance або student runtime refs | **RESOLVED** |

### Результати повторного прогону

| Перевірка | Фактично | Результат |
| --- | --- | --- |
| `pnpm exec tsc --noEmit` | exit 1: `info/testing/schedule-v2.integration.test.ts:175` передає DOM `Request` до handler, що приймає `NextRequest` | FAIL |
| `pnpm test` | exit 0; 10 files: 9 passed, 1 skipped; 79 tests: 73 passed, 6 DB integration skipped | PASS для unit suite; skipped integration не зараховано |
| `pnpm build` | exit 1 після успішного compile; той самий TS2345 `Request` → `NextRequest` | FAIL |
| `pnpm test:schedule-v2-integration` | exit 1; suite setup failed на `current_schema()` guard; 6/6 tests skipped | FAIL |
| Migration 014 first/repeat | setup не дійшов до migration, бо Neon HTTP connection не застосував URL `options=-csearch_path=…` | BLOCKED |
| JSON 431 preview/import/idempotency/update/rollback | integration tests підготовлені, але 6/6 skipped через harness setup | BLOCKED |
| Auth/session, CRUD, conflicts, move/replacement/cancel/one-time, public API | integration tests підготовлені, але не виконані | BLOCKED |
| Browser isolated runner | сервер не можна безпечно запустити через schema-scoping failure; додатково default `pnpm start -- -p 3013` відтворено окремо й він падає як invalid project directory `...\\-p` | FAIL / BLOCKED |
| UI 1440/820/390, keyboard, console, document overflow | isolated app server не стартував; browser flow не виконувався і не позначений PASS | BLOCKED |
| Legacy runtime scan | збігів немає | PASS |
| Diff hygiene | `git diff --check` без whitespace errors; lockfile не змінено; secret scan знайшов лише documented env placeholder та фіктивні `example.test` QA credentials | PASS |

### Нові підтверджені дефекти

#### QA-029-04 · VID-029 · P1 — isolated schema connection не працює з поточним Neon transport

- Кроки: виконати `pnpm test:schedule-v2-integration` з чинним `.env.local`.
- Очікувано: випадкова `qa_vid029_*` schema стає `current_schema()`, migration/tests виконуються, schema очищається.
- Фактично: `info/testing/schedule-v2-test-database.mjs:21` додає URL `options=-csearch_path=...`, але Neon HTTP session зберігає `"$user", public`; guard на `:133` кидає «Не вдалося довести ізоляцію search_path». 6 тестів skipped.
- Додатковий read-only доказ: обидві форми `-csearch_path=pg_catalog` і `-c search_path=pg_catalog` залишили `current_schema=public` та `search_path="\"$user\", public"`.
- Безпека: guard спрацював до migration; cleanup підтверджено, production fingerprint не змінився.
- Вплив: migration/import/CRUD/auth/public integration та browser QA неможливі; release gate закритий.

#### QA-029-05 · VID-029 · P1 — integration test ламає typecheck і production build

- Кроки: `pnpm exec tsc --noEmit` або `pnpm build`.
- Очікувано: exit 0.
- Фактично: TS2345 у `info/testing/schedule-v2.integration.test.ts:175`: стандартний `Request` не сумісний із параметром `NextRequest` route handler.
- Вплив: два обов’язкові release checks падають; deployment заборонений.

#### QA-029-06 · VID-029 · P1 — default browser runner не може запустити Next production server у Windows

- Кроки: запустити wrapper без explicit command або виконати його default child command `pnpm start -- -p 3013`.
- Очікувано: `next start` слухає порт 3013.
- Фактично: `scripts/with-isolated-schedule-v2-db.mjs:14` формує `['start','--','-p','3013']`; pnpm передає literal `"--"` у Next, і Next завершується exit 1: `Invalid project directory ...\\-p`.
- Вплив: навіть після виправлення DB scope передбачений isolated browser flow не стартує; UI/auth/public/admin acceptance не може бути підтверджено.

### Прибирання повторного прогону

- Після failed integration: `qa_vid029_*` schemas = 0; `pgcrypto` лишився відсутнім; public row fingerprint незмінний.
- Product DB tables/rows, користувачі, sessions і production migration не змінювалися.
- Browser tabs і server processes не створювалися, оскільки isolated server не пройшов precondition; порт 3013 не відкривався.
- Залишено лише дозволений fingerprint artifact `verified-files-rerun.tsv`.

### Актуальний висновок повторного прогону

- Результат пакета: `FAIL`.
- Виправлено: QA-029-01 і QA-029-03.
- Лишається відкритим: QA-029-02, оскільки додане покриття не виконалося.
- Нові блокуючі дефекти: QA-029-04, QA-029-05, QA-029-06.
- Обов’язкові неперевірені критерії: migration 014, DB import/rollback/idempotency, CRUD, auth/session, exceptions/public API, повний isolated browser/UI flow 1440/820/390.
- Актуальність: тільки `HEAD 5a632ad851f1505b3cc0965d4fe529a1f0d0bf94` + dirty/untracked manifest SHA-256 `650b7723a1112d7ad61ff5e40c3fc37b24bad218daef46b46cd086258d74fc95`.
- QA-gate: **закритий**. Push, deployment і production migration не дозволені цим повторним прогоном.

---

## Другий повторний прогін після виправлень QA-029-04/05/06 — 02.09.2026

### Контекст і safety preflight

- Час: 15:30–15:33 EEST.
- QA-агент: `/root/qa_vid_029`, `gpt-5.6-sol`, reasoning `high`.
- Перевірюваний стан: `HEAD 5a632ad851f1505b3cc0965d4fe529a1f0d0bf94` плюс актуальний dirty/untracked scope.
- Актуальний manifest: 252 продуктові/документаційні файли у `info/qa/artifacts/QA-20260902-02/verified-files-rerun-2.tsv`; SHA-256 `1c3d4a7775952c7c3c61d9f8d7afdbf2e567dfc166c03630293af328ceab71a0`. QA report/artifacts та QA-mutated `info/tasks.md` виключено з продуктового відбитка.
- Повторно прочитано handoff, поточний `info/tasks.md`, попередні append-only докази, `qa-batch` і `ui-spacing-review`.
- Статично підтверджено випадкову схему `qa_vid029_${randomBytes(8).toString("hex")}`, allowlist `/^qa_vid029_[a-f0-9]{16}$/`, `current_schema()` guard до migration/seed, cleanup через `DROP SCHEMA IF EXISTS <validated-random-name> CASCADE` і QA-only активацію scoped client через `QA_TEST_SCHEMA`.
- Harness більше не покладається на URL `options`: кожен query/transaction виконує `set_config('search_path', qa_schema, true)` у тій самій транзакції. У продуктових викликах немає `.unsafe` або явних `public.*` write references; незаскоплений `unsafe`-метод поточним пакетом не використовується.
- Read-only baseline: `current_schema=public`, `pgcrypto=false`, 18 public base tables, `qa_vid029_*` schemas = 0, SHA-256 точних row counts `eb59d249fef1a49d386be3f980e1abbc0745176a53357cfcdbe061c29cfccccb`.

### Результати команд і сценаріїв

| Перевірка | Фактично | Результат |
| --- | --- | --- |
| `pnpm test:schedule-v2-integration` | exit 0; 1 file, 6/6 tests passed за 17.82 s | PASS |
| Migration 014 first/repeat | обидва застосування у випадковій QA-схемі завершилися | PASS |
| Auth/session | валідні admin credentials, hashed session create/read/delete | PASS |
| JSON import | actual 431-record preview, import, idempotency, update і transactional rollback | PASS |
| CRUD / schedule rules | усі catalog kinds; conflicts; move, replacement, cancel, one-time | PASS |
| Public API | route викликано через `NextRequest`; payload успішний і без administrator fields | PASS |
| DB cleanup / production safety | після integration `qa_vid029_*` schemas = 0; `pgcrypto=false`; 18 tables; row fingerprint дослівно незмінний | PASS |
| `pnpm exec tsc --noEmit` | exit 1; `lib/db.ts:35`, TS2345 у generic типах `NeonQueryPromise` для `scope(client.query(...))` | **FAIL** |
| `pnpm test` | exit 0; 10 files: 9 passed, 1 skipped; 79 tests: 73 passed, 6 integration skipped у загальному suite | PASS для unit suite; DB suite окремо PASS вище |
| `pnpm build` | compile завершився, typecheck exit 1 на тому самому `lib/db.ts:35` TS2345 | **FAIL** |
| Isolated browser runner `pnpm start --port 3013` | не запускався: current package не має успішного production build; використання наявного `.next` перевіряло б stale artifact | NOT_RUN через blocking defect |
| Browser/auth/public/admin/UI 1440/820/390 | не виконувався без валідного current production build; PASS не заявляється | NOT_RUN |

### Статус попередніх дефектів

| Дефект | Другий повтор | Статус |
| --- | --- | --- |
| QA-029-04 — schema scoping через Neon HTTP URL options | Transactional `set_config` підтверджено 6/6 integration tests; cleanup і незмінність public fingerprint доведені | **RESOLVED** |
| QA-029-05 — `Request` / `NextRequest` у integration test | Integration route тепер використовує `NextRequest`; попередня помилка зникла | **RESOLVED** |
| QA-029-06 — Windows browser runner arguments | Код default command тепер формує `pnpm start --port 3013`; runtime acceptance не виконано через новий build failure | **CODE FIX CONFIRMED / RUNTIME NOT_RUN** |
| QA-029-02 — фактичне DB integration coverage | Усі 6 сценаріїв реально виконані й пройшли | **RESOLVED** |

### Новий блокуючий дефект

#### QA-029-07 · VID-029 · P1 — QA-scoped DB wrapper ламає typecheck і production build

- Кроки: виконати `pnpm exec tsc --noEmit` або `pnpm build`.
- Очікувано: exit 0; QA-only wrapper типізується без послаблення strict TypeScript.
- Фактично: `lib/db.ts:35` передає результат generic `client.query(...)` у `scope`, який приймає лише `NeonQueryPromise<false, false>`. TypeScript повертає TS2345 через несумісність `ArrayModeOverride` / `FullResultsOverride`; `pnpm build` завершується `Failed to type check`.
- Вплив: два обов’язкові release checks падають; немає валідного production build для isolated browser/auth/UI acceptance; release gate закритий.

### Прибирання і висновок

- Фінальна read-only перевірка після integration/unit/build: `current_schema=public`, `pgcrypto=false`, 18 public tables, fingerprint `eb59d249fef1a49d386be3f980e1abbc0745176a53357cfcdbe061c29cfccccb`, QA schemas = 0.
- Production/public rows, extensions і schema не змінено; production migration, commit, push і deployment не виконувалися.
- Результат пакета: **FAIL**.
- QA-029-04, QA-029-05 і фактичне integration coverage виправлені; QA-029-06 статично виправлено, але browser runner не перевірений на current build.
- Відкритий блокуючий дефект: **QA-029-07**.
- Browser/auth/public/admin/UI та `ui-spacing-review` на 1440/820/390 залишаються `NOT_RUN`, бо поточний production build відсутній. Це не перетворює результат на `BLOCKED`: у пакеті є відтворюваний продуктовий `FAIL`.
- QA-gate: **закритий**. Push, deployment і production migration не дозволені цим прогоном.

---

## Третій повторний прогін після виправлення QA-029-07 — 02.09.2026

### Контекст і safety

- Час: 15:36–15:43 EEST.
- QA-агент: `/root/qa_vid_029`, `gpt-5.6-sol`, reasoning `high`.
- Стан: `HEAD 5a632ad851f1505b3cc0965d4fe529a1f0d0bf94` плюс актуальний dirty/untracked scope.
- Manifest: 252 продуктові/документаційні файли у `info/qa/artifacts/QA-20260902-02/verified-files-rerun-3.tsv`; SHA-256 `fcd0d0f94d960af2327c6b180d6887b4cf560d0afa53e2fc1a0376856f03c626`. QA report/artifacts та QA-mutated `info/tasks.md` виключено.
- До, після integration і після browser-server cleanup: `current_schema=public`, `pgcrypto=false`, 18 public tables, QA schemas = 0, row-count fingerprint `eb59d249fef1a49d386be3f980e1abbc0745176a53357cfcdbe061c29cfccccb`.
- Production/public tables, rows, extensions і production migration не змінювалися.

### Результати

| Перевірка | Фактично | Результат |
| --- | --- | --- |
| `pnpm exec tsc --noEmit` | exit 0 | PASS |
| `pnpm test` | exit 0; 10 files: 9 passed, 1 skipped; 79 tests: 73 passed, 6 DB integration skipped у загальному suite | PASS для unit; DB suite окремо нижче |
| `pnpm build` | exit 0; compile, TypeScript, page data і 19/19 static pages успішні | PASS |
| `pnpm test:schedule-v2-integration` | exit 0; 1 file, 6/6 tests passed за 15.97 s | PASS |
| DB scenarios | migration 014 first/repeat; auth/session; actual JSON preview/import/idempotency/update/rollback; CRUD; conflicts; move/replacement/cancel/one-time; public API | PASS |
| Default isolated browser wrapper | створив `qa_vid029_bed952b044bbfddf`, потім `spawn pnpm.cmd` синхронно впав `EINVAL`; wrapper exit 1 | **FAIL** |
| Default wrapper cleanup | не виконався, бо `spawn(...)` розташовано перед `try/finally`; schema залишилася до ручного QA cleanup | **FAIL** |
| Explicit safe workaround | `with-isolated-schedule-v2-db.mjs node node_modules/next/dist/bin/next start --port 3013`; current build READY на 3013; schema `qa_vid029_f92fc765adbb99b2` автоматично прибрана після SIGINT | PASS як діагностичний workaround, не виправлення default runner |
| Read-only HTTP smoke | `/schedule?date=2026-09-02` 200, містить QA-1 і не містить `/admin` link; `/admin` 307 → `/admin/login`; login 200; groups/public schedule API успішні, містять QA fixture і не містять admin fields | PASS |
| Real browser selection | browser runtime не виявив жодного доступного backend (`[]`) після обов’язкового troubleshooting | BLOCKED (environment) |
| Browser admin login/logout/full flow | реальний браузер недоступний; не підмінено HTTP smoke | BLOCKED |
| `ui-spacing-review` 1440/820/390 | viewport measurements, keyboard, console і document overflow неможливі без browser backend | BLOCKED |
| Legacy runtime scan | `/dashboard`, signup, journal, attendance і student runtime refs не знайдені | PASS |
| Diff/secret hygiene | `git diff --check` без errors; lockfile не змінено; literal secrets не знайдені | PASS |

### Статус QA-029-07

**RESOLVED.** Явне звуження внутрішнього `client.query` promise у `lib/db.ts` прибрало TS2345 без runtime-регресії: typecheck і build exit 0, DB integration 6/6 PASS.

### Новий дефект

#### QA-029-08 · VID-029 · P1 — default Windows browser runner падає до захищеного cleanup

- Кроки: у Windows виконати `node --env-file=.env.local scripts/with-isolated-schedule-v2-db.mjs`.
- Очікувано: wrapper запускає `pnpm start --port 3013`; незалежно від child success/error гарантовано видаляє власну випадкову QA-схему.
- Фактично: після створення `qa_vid029_bed952b044bbfddf` `spawn("pnpm.cmd", ["start", "--port", "3013"], { stdio: "inherit" })` синхронно кидає `Error: spawn EINVAL`. Оскільки `spawn` виконується на `scripts/with-isolated-schedule-v2-db.mjs:21` до `try/finally` на рядку 39, `destroyScheduleV2TestDatabase` не викликається; schema лишилася.
- Cleanup evidence: QA вручну видалив лише точну схему через валідований `destroyScheduleV2TestDatabase`; після цього QA schemas = 0, public fingerprint незмінний.
- Вплив: документований/default browser QA runner не працює у цільовому Windows-середовищі та порушує гарантію автоматичного cleanup. Release gate закритий.

### Неперевірене та прибирання

- Через відсутній browser backend не виконані real admin login/logout, keyboard traversal, browser console, document overflow та фактичні spacing measurements на 1440/820/390. Вони мають статус `BLOCKED`, не PASS.
- Default-run schema `qa_vid029_bed952b044bbfddf` видалено вручну після дефекту. Workaround schema `qa_vid029_f92fc765adbb99b2` видалена wrapper-ом. Порт 3013 вільний; серверних процесів QA не залишено.
- Browser tabs не створено, оскільки browser backend був недоступний.
- Commit, push, deployment і production migration не виконувалися.

### Актуальний висновок

- Результат пакета: **FAIL**.
- QA-029-07 виправлено; mandatory typecheck/unit/build та DB integration пройшли.
- Відкритий дефект: **QA-029-08 (P1)**.
- Browser/auth/UI acceptance і `ui-spacing-review` залишаються `BLOCKED` через недоступний browser backend.
- Актуальність: `HEAD 5a632ad851f1505b3cc0965d4fe529a1f0d0bf94` + manifest SHA-256 `fcd0d0f94d960af2327c6b180d6887b4cf560d0afa53e2fc1a0376856f03c626`.
- QA-gate: **закритий** через відтворюваний P1 runner/cleanup defect і незавершені обов’язкові browser/UI checks.

---

## Четвертий повторний прогін після виправлення QA-029-08 — 02.09.2026

### Контекст, стан і безпека

- Час: 15:45–16:05 EEST.
- QA-агент: `/root/qa_vid_029`, `gpt-5.6-sol`, reasoning `high`.
- Перевірений стан: `HEAD 5a632ad851f1505b3cc0965d4fe529a1f0d0bf94` плюс актуальний dirty/untracked scope.
- Manifest: 252 продуктові/документаційні файли у `info/qa/artifacts/QA-20260902-02/verified-files-rerun-4.tsv`; SHA-256 `d0c0ed573881205e087e274f4cce08f7942105233ade26f55f41c05f22ab5a5a`. QA report/artifacts та QA-mutated `info/tasks.md` виключено.
- Baseline і фінальна перевірка: `current_schema=public`, `pgcrypto=false`, 18 public base tables, `qa_vid029_*` schemas = 0, public row-count fingerprint `eb59d249fef1a49d386be3f980e1abbc0745176a53357cfcdbe061c29cfccccb`.
- Усі записи import/browser виконувалися лише у випадкових QA-схемах. Production/public data, extensions і migration не змінювалися.

### Mandatory gates і DB acceptance

| Перевірка | Фактично | Результат |
| --- | --- | --- |
| `pnpm exec tsc --noEmit` | exit 0 | PASS |
| `pnpm test` | exit 0; 10 files: 9 passed, 1 skipped; 79 tests: 73 passed, 6 DB tests штатно skipped у загальному suite | PASS для unit |
| `pnpm build` | exit 0; compile, TypeScript, page data, 19/19 static pages | PASS |
| `pnpm test:schedule-v2-integration` | exit 0; 1 file, 6/6 tests passed за 21.29 s | PASS |
| DB acceptance | migration 014 first/repeat; auth/session; actual JSON preview/import/idempotency/update/rollback; CRUD; conflicts; move/replacement/cancel/one-time; public API | PASS |
| Legacy/diff hygiene | runtime legacy refs відсутні; `git diff --check` clean; lockfile незмінний; literal secrets не знайдені | PASS |

### QA-029-08 і default runner

- `node --env-file=.env.local scripts/with-isolated-schedule-v2-db.mjs` без аргументів запустив default `pnpm start --port 3013`; Next 16.3.3 став READY.
- Перший контрольний запуск: schema `qa_vid029_0ea735bf71e0fff7`; після SIGINT wrapper надрукував `QA schema removed`, schema list = `[]`, порт 3013 вільний.
- Другий default запуск для browser QA: schema `qa_vid029_66bb20b07e21a084`; після завершення Playwright flow SIGINT знову викликав автоматичний cleanup, schema list = `[]`, порт 3013 вільний.
- Отже QA-029-08 **RESOLVED**: Windows launch і гарантований cleanup після контрольованого завершення фактично підтверджені.
- Неблокуюче спостереження: Node 24 друкує `[DEP0190]` для `spawn(..., { shell: true })`. Це warning локального QA launcher, не browser/application error і не порушує перевірені start/cleanup критерії; рекомендовано прибрати deprecated invocation у наступному технічному пакеті.

### Real browser / auth / import flow

Інтегрований browser backend повторно був недоступний (`No browser is available`). За прямим дорученням використано terminal Playwright CLI через `npx --package @playwright/cli playwright-cli`, реальний headed Microsoft Edge 152, окрема session `vid029r4`.

Фактично пройдено:

- `/schedule?date=2026-09-02`: public QA-1, тестова дисципліна/викладач/аудиторія, day → week navigation; public header без admin/login link.
- `/transfers`: сторінка та її локально scrollable table region завантажуються.
- `/admin` без сесії редиректить на `/admin/login`.
- Невірний пароль повертає generic «Невірна електронна адреса або пароль адміністратора.» без розкриття облікового запису.
- Валідні фіктивні credentials відкривають єдину admin panel; schedule/catalog/navigation доступні.
- `/admin/import`: actual `teacher-schedule-lessons.json`, preview 431, 34 teachers, 53 disciplines, 17 rooms, 159 groups, 3 lesson types; conflict warnings visible; explicit checkbox і confirm import. UI: «Імпорт завершено: створено 431…»; scoped DB evidence: 431 `schedule_exceptions`, 431 `schedule_import_items`, 1 `schedule_import_runs`.
- Mobile navigation має `dialog` з доступною назвою; Escape закриває drawer і повертає focus на «Відкрити меню».
- Logout повертає на public schedule; повторний `/admin` знову редиректить на login.
- Public і admin focus order доступний з клавіатури, `:focus-visible` підтверджений на links/selects/inputs/buttons. Подвійні stops усередині native date inputs належать browser subcontrols.

### `ui-spacing-review`

| Viewport | Public schedule | Admin schedule | Overflow / touch |
| --- | --- | --- | --- |
| 1440×900 | main `x=60`, width `1320`; controls 44 px | sidebar 272 px; content starts `x=328` (56 px inset); table region `1039/1120` | document `1440/1440`; table scroll only inside `overflow-x:auto` |
| 820×900 | main `x=16`, width `788`; controls 44 px | content inset 24 px; region `755/1120` | document `805/805`; no page overflow |
| 390×844 | main `x=12`, width `351`; navigation/form controls 44 px | content inset 16 px; region `342/1120`; search label touch area 44 px | document `375/375`; no page overflow |

Додатково `/transfers` на 390 px: document `390/390`; table region `357/760`, `overflow-x:auto`. Відступи послідовно зменшуються 60/56 → 16/24 → 12/16; контент не торкається країв і не перекривається shell/navigation. Підтверджених spacing-дефектів немає.

### Console і відомий локальний baseline

- Browser console: 2 errors, 0 warnings; обидва мають один відомий локальний root — `/_vercel/insights/script.js` 404 і похідний MIME refusal, коли локальний `next start` читає `VERCEL=1` із `.env.production.local` поза Vercel edge.
- `app/layout.tsx` з умовою `process.env.VERCEL === "1"` не змінювався від base HEAD; цей local Analytics 404 вже зафіксований у `QA-20260828-04`. Нових application errors/warnings під час public/login/import/admin/logout flow немає.
- На Vercel deployment цей endpoint надається платформою; deployment у цьому QA не запускався.

### Прибирання і висновок

- Playwright session закрита; generated `.playwright-cli` видалено; browser sessions = 0.
- Обидві QA-схеми default runner автоматично видалені; фінально QA schemas = 0, порт 3013 вільний, public fingerprint незмінний.
- Commit, push, deployment і production migration не виконувалися.
- Нових блокуючих дефектів не знайдено; QA-029-01…08 закриті для актуального scope.
- Результат пакета: **PASS**.
- Неперевірених або заблокованих обов’язкових критеріїв немає.
- Актуальність: `HEAD 5a632ad851f1505b3cc0965d4fe529a1f0d0bf94` + manifest SHA-256 `d0c0ed573881205e087e274f4cce08f7942105233ade26f55f41c05f22ab5a5a`.
- QA-gate для цього точного стану: **відкритий**. Це не є окремим дозволом на commit, push, deployment або production migration.
