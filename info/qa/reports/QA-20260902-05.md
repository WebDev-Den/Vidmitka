# QA-звіт: QA-20260902-05

## Контекст і межі

- Тригер: прямий запит користувача «перевір чи все реалізовано і залий» перед можливим push у `main`; 02.09.2026, Europe/Kyiv.
- ID QA-агента: `/root/qa_vid033_036`; модель `gpt-5.6-sol`, reasoning `high` (календарні правила, адміністративні права та DB integration).
- Задачі: VID-033, VID-034, VID-035, VID-036; VID-037 — релізна дія після позитивного QA.
- База й HEAD: `51266c9e58ed6139bcf1b7f8b66306558db82a68` (`origin/main` збігається з HEAD). Перевірено весь dirty worktree: 24 product/test/info-файли (14 tracked changed, 10 untracked); QA-звіт і артефакти виключено з відбитка.
- Відбиток scope: SHA-256 кожного файлу зафіксовано під час прогону; перелік включає всі 24 файли з `git diff --name-only` та `git ls-files --others --exclude-standard`, зокрема `calendar-override-*`, `calendar-overrides.ts`, зміни public/admin UI і integration test. `git diff --check` — без помилок; пошук секретів у scope не знайшов connection strings, токенів або private keys.
- Середовище: локальна збірка Next.js 16.3.3; DB integration створював схему `qa_vid029_<16 hex>` та видалив її у `afterAll`. Production-дані, env, ролі, міграції та deployment не змінювалися.
- Виключено: commit, push, Vercel deployment і застосування 12 дат до production — заборонені дорученням.

## Класифікація і план

| Задача | Тип, визначений QA | Підстава | Ризик | Сценарії та регресії |
| --- | --- | --- | --- | --- |
| VID-033 | Фіча (UI / IA) | Нова компактна admin-оболонка | Втрата маршруту, keyboard/drawer regression | build, статичний маршрутний review, browser admin desktop/mobile |
| VID-034 | Фіча (календар) | Новий CRUD і глобальна підміна дня | Помилкові дати, права, journal protection, resolver | pure rules, isolated CRUD/context/template, public/admin browser |
| VID-035 | Фіча (public UX) | Прибрано global group filter, додано context картки | Прихована втрата груп/паралельних занять, focus/overflow | unfiltered day, cards, hover/focus/touch, navigation |
| VID-036 | Фіча (public filter) | Новий active-teacher filter | Неправильний exception filter або втрачений query | positive/negative/stale ID, day/week/navigation, keyboard/overflow |
| VID-037 | Технічна задача (QA/release) | Потрібен актуальний незалежний gate | Публікація невалідного пакета | повний command, DB, UI та diff gate |

## Результати критеріїв

| Задача / критерій | Перевірка або команда | Очікувано | Фактично / доказ | Статус |
| --- | --- | --- | --- | --- |
| Увесь пакет | `git diff --check`, dirty/untracked scope, secret scan | Чистий diff без секретів/артефактів | whitespace-помилок і збігів секретів не знайдено; scope містить лише погоджені product/test/info файли та QA-артефакти | PASS |
| VID-034 pure rules | `pnpm test` | Точні 12 дат і валідація | `calendar-override-rules.test.ts` пройшов у загальному suite | PASS |
| VID-033–036 unit | `pnpm test` | Unit suite без помилок | exit 0: 10 files passed, 76 tests passed; 1 file / 8 tests skipped (DB integration без opt-in) | PASS |
| VID-033–036 typecheck | `pnpm exec tsc --noEmit` | Strict TypeScript без помилок | exit 0 | PASS |
| VID-033–036 production build | `pnpm build` | Production compilation/data collection | exit 0; 19 routes generated, dynamic `/admin`, `/schedule` and API routes present | PASS |
| VID-034, VID-035, VID-036 DB integration | `pnpm test:schedule-v2-integration` | Isolated DB CRUD/context/template/public filter | exit 1: 4 passed, 4 failed. Missing `get_schedule_day(date)` stops public day/filter/API; missing `lock_schedule_day(date)` stops calendar CRUD. | FAIL |
| VID-034 browser `/admin/exceptions`, `/transfers`, transfer day | Real browser at 1440/820/390, keyboard/console/overflow | Admin CRUD, public table and transfer resolver verified | Not completed: the available Python runtimes lack `playwright`; `agent-browser` CLI absent; remote Edge launch is policy-blocked. The initial Playwright script and its failed no-data attempt are retained in `info/qa/artifacts/QA-20260902-05/`. | BLOCKED |
| VID-033 browser admin shell | Real browser at 1440/820/390, collapsed/drawer/focus trap | All routes, desktop collapse and mobile dialog | BLOCKED for the same browser-runtime limitation; no authenticated production session was created. | BLOCKED |
| VID-035–036 public browser | Real browser at 1440/820/390, hover/focus/touch, filter query, console/overflow | Context groups and teacher filter verified | BLOCKED for the same browser-runtime limitation. A local `next start` did start successfully for the attempted read-only check and was then stopped; no browser assertion executed. | BLOCKED |
| UI spacing review | Changed TSX/CSS + real 1440/820/390 measurements | Rhythm, targets, overlap, document overflow | BLOCKED: no Playwright/agent-browser runtime available; therefore no actual `getBoundingClientRect()` or console measurements may be claimed. | BLOCKED |

## Потребує виправлень

### QA-05-01 — P1 — ізольований schedule-v2 harness не застосовує залежність migration 009

- Пов’язані задачі: VID-034, VID-035, VID-036.
- Кроки: виконати `pnpm test:schedule-v2-integration`.
- Очікувано: isolated schema має містити чинні `makeup_days`, `get_schedule_day(date)` і `lock_schedule_day(date)`, тому перевіряються calendar CRUD, public day/week, group context та teacher filter.
- Фактично: `info/testing/schedule-v2-test-database.mjs` виконує тільки `db/migrations/014_schedule_v2.sql`. Функції й `makeup_days` є в `db/migrations/009_makeup_days.sql`, тому 4 із 8 integration tests падають з PostgreSQL `42883`; API assertion отримує 503 замість 200.
- Вплив: незавершена обов’язкова ізольована DB-перевірка не доводить ключову функцію пакета; release gate закритий.

## Неперевірене й обмеження

- Повний browser/UI QA, включно з keyboard, console, document-level overflow, touch targets і spacing-вимірами на 1440/820/390, не може бути завершений у наданому runtime. Це не позначено успішним статичним review.
- Через FAIL DB integration не можна підтвердити template fallback для імпортованих дат, precedence actual teachers in exceptions, або end-to-end filter/navigation.

## Прибирання

- Isolated integration schema створювалася лише під дозволеним префіксом `qa_vid029_…` і була видалена `afterAll`.
- Локальний `next start` на port 3013, запущений для read-only browser attempt, зупинено; порт після цього вільний.
- Залишено лише QA-артефакт `browser-ui-qa.py` без персональних даних або секретів; результати не сформовані, бо Python не має модуля Playwright.

## Висновок

- Результат пакета: **FAIL**.
- Підтверджено: TypeScript, unit suite, production build, exact pure list of 12 dates, diff/secrets hygiene.
- Потребує виправлень: QA-05-01 (P1), після чого потрібен повторний isolated DB integration; UI критерії також потребують реального browser runtime і повного повторного QA.
- Актуальність: лише для dirty scope від `51266c9e58ed6139bcf1b7f8b66306558db82a68`, відбитого вище. Будь-яка зміна product/test scope робить результат неактуальним.
- QA-gate для релізу: **закритий**. Commit, push у `main`, Vercel deployment і production batch перенесень не дозволені.

## Повторний прогін після QA-05-01 — 02.09.2026

Основний агент виправив лише `info/testing/schedule-v2-test-database.mjs`: harness створює мінімальну `attendance_sessions`, застосовує чинну `009_makeup_days.sql`, а потім застосовує `014_schedule_v2.sql` двічі. Product-код, залежності та lockfile не змінювалися. Це цільово усуває P1 QA-05-01.

| Перевірка | Фактичний результат | Статус |
| --- | --- | --- |
| Harness diff | Мінімальні prerequisite table + actual migration 009 перед 014; `git diff --check` чистий | PASS |
| `pnpm test:schedule-v2-integration` | exit 0; 1 file, **8/8 tests passed**; schema з безпечним префіксом створено й прибрано | PASS |
| Firefox browser на test schema | `node --env-file=.env.local scripts/with-isolated-schedule-v2-db.mjs pnpm start --port 3013`; login фіктивного QA-admin; `/admin`, `/admin/exceptions`, `/transfers`, `/schedule` | PASS |
| VID-034 browser | Batch confirm зберіг 12/12 у QA-схемі; `/transfers` показує 12 хронологічних рядків і links; 04.09 має «За розкладом: Понеділок» / «Чисельник» | PASS |
| VID-033 browser/keyboard | Desktop expand/collapse, index routes, mobile dialog; Tab/Shift+Tab trap, Escape і focus return підтверджено | PASS |
| VID-035–036 browser | Group context перевірено у hover desktop і має безпечний mobile layout; `teacher` фільтр вибирає QA-teacher, зберігається в day/week/tabs/arrows; stale UUID скидається до «Всі викладачі» | PASS |
| Responsive/UI spacing | 1440/820/390: document clientWidth=scrollWidth. Teacher select 44 px; 390 admin table 720 px прокручується тільки у власному 356 px контейнері; 820/1440 table container не переповнений. Деталі: `info/qa/artifacts/QA-20260902-05/firefox-retest.md`. | PASS |
| Console | Виявлена тільки відома локальна відмова `/ _vercel/insights/script.js` Vercel Analytics через `text/plain`; вона повторюється на всіх pre-existing routes і не походить з product diff. Нових помилок/warnings пакета не виявлено. | PASS |

### Прибирання повторного прогону

- Firefox session `qa-runtime` закрито.
- `SIGINT` зупинив `next start`; port 3013 вільний, а harness прибрав схему `qa_vid029_059a0a106c9f6c3d`.
- Залишено тільки безпечний текстовий QA-артефакт `info/qa/artifacts/QA-20260902-05/firefox-retest.md`; browser snapshots лишаються в ignored `.playwright-cli/`.

### Актуалізований висновок

- Результат пакета: **PASS**.
- QA-05-01 закрито: isolated DB integration 8/8 пройшов.
- Всі обов’язкові критерії VID-033–VID-036 підтверджено; production не змінювалася. Актуальний список 25 SHA-256 контрольних сум: `info/qa/artifacts/QA-20260902-05/scope-sha256.txt`.
- QA-gate для конкретного поточного dirty scope: **відкритий**. Це не є commit/push/deployment; вони залишаються окремими авторизованими діями.

Примітка до browser console: шлях локального Analytics script — `/_vercel/insights/script.js` (без пробілу); це pre-existing local integration warning, а не дефект пакета.

## Статична перевірка release-metadata після PASS — 02.09.2026

Після успішного повторного прогону змінено тільки release-metadata у `info/tasks.md`: прибрано випадково дубльований заголовок `## VID-036` і уточнено, що QA PASS, а публікація очікує окремих commit / push / deployment. Product-код, тестовий harness, залежності, lockfile, критерії приймання й production scope не змінювалися.

- Статично підтверджено рівно один заголовок `## VID-036`; застарілого стану «QA виконується» немає.
- `git diff --check` пройшов без помилок.
- Повторні typecheck, unit, build, isolated integration та browser/UI QA не запускалися: metadata-only уточнення не змінює перевірену поведінку. PASS повторного прогону лишається чинним.
- Оновлено лише контрольну суму `info/tasks.md` у повному маніфесті scope: `60c5c2a0be8b531805ef879be030adde30f258a7fc4fa318d3a9631c99655db0`; усі product/test записи маніфесту без змін.

### Release gate

- Результат: **PASS**.
- QA-gate: **відкритий** для перевіреного поточного пакета. Commit, push і deployment не виконувалися та потребують окремої авторизації.
