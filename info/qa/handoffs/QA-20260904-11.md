# QA-20260904-11 — VID-061 admin QStash cron

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`, `.agents/skills/qa-batch/SKILL.md`, `info/qa/process.md`, `info/qa/handoff-template.md`, `info/design-system.md` і задачу VID-061 у `info/tasks.md`. Ти вже QA-агент: не делегуй цей QA іншому агенту.

Тригер: прямий запит користувача «Можеш задеплоїти і перевірити чи всього вистачає» перед дозволеним production release.

Пакет: `QA-20260904-11`.

Задача: VID-061 — фіча admin status/ініціалізації QStash cron. Первинний запит, критерії, TDD seams і операційні межі є в `info/tasks.md`.

База: `62d5c02` (`main`, останній production release). Перевірюваний стан: поточний `HEAD` плюс усі tracked і untracked зміни для VID-061. До обсягу входять нові `app/admin/(protected)/cron/`, `lib/public-push/qstash-schedules.ts`, unit-тести, admin navigation, CSS та документація; не включай старі browser/iPhone `BLOCKED` VID-060.

Середовище: Vercel Production має encrypted names `QSTASH_URL`, `QSTASH_TOKEN`, `PUSH_SCANNER_URL`, дві QStash signing keys і VAPID variables. Не витягуй, не друкуй і не передавай їхні значення. Не викликай QStash, production app, production DB, schedules або push provider. Тимчасовий one-minute probe є лише post-release operational step основного агента після PASS.

Режим: лише QA; продукт не редагувати, push/deploy/production migration заборонені. Дозволені записи: цей QA-звіт, QA-поля VID-061 у `info/tasks.md`, необхідні тести в узгодженому обсязі.

Перевір самостійно:

1. критерії VID-061: server-only config, reject arbitrary QStash URL, fixed IDs/cron/POST/body, no duplicate semantics, safe metadata labeling, no secret in UI/result/log code;
2. authorization boundary сторінки та Server Action;
3. `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build` і відсутність секретів/generated artifacts у diff;
4. UI browser flow, keyboard/console/document overflow на 1440, 820, 390 і 430 px, якщо доступний runtime; для UI прочитай і застосуй `ui-spacing-review`. Якщо runtime відсутній, запиши фактичний blocker, не вигадуй PASS;
5. достатність Production env підтверджуй лише за назвами, не значеннями.

Створи `info/qa/reports/QA-20260904-11.md` за шаблоном, онови лише QA-поля VID-061 та поверни PASS/FAIL/BLOCKED, точний перевірений стан, виконане, дефекти/блокери й чи дозволяє звіт release.
