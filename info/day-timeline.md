# Шкала навчального дня — VID-007

У VID-024 нативний picker і HEX-правила повторно використано для кольорів типів занять: спільні `components/color-field.tsx`, `components/color-field.module.css`, `lib/ui/colors.ts`. Чинний `PeriodColorField` лишається сумісною обгорткою; кольори часової шкали й кольори типів незалежні. Регресію редактора пар включено до наступного пакетного QA. Історичні шляхи нижче відображають попередні версії реалізації.

Статус на 28.08.2026: **QA PASS, опубліковано** у релізі `efcc141`. Міграція 011 застосована після незалежної перевірки, включно з міграційним backfill/reapply, таймером, перервами, північчю, reduced motion та UI 1440/820/390. Первинний запит — у [журналі задач](tasks.md), докази — у [QA-звіті](qa/reports/QA-20260828-01.md).

## Поведінка

У публічній верхній частині головної й розкладу показується спільна шкала дзвінків — від початку першої активної пари до кінця останньої. Пари впорядковані за часом, їх номери зберігаються; перерви — фактичні проміжки між слотами. Деактивованих пар немає. Це часовий орієнтир, незалежний від наявності занять, чисельника/знаменника та вибраної дати перегляду розкладу. Напис «Сьогодні · …» відділяє живий час від перегляду іншої дати. За VID-017 годинник і доступний опис шкали не містять назви міста; часовий пояс `Europe/Kyiv` незмінний. Ця правка реалізована, незалежний QA очікується (`NOT_RUN`); критерії — у [журналі задач](tasks.md#vid-017--підписи-часу-без-назви-міста).

Маркер не є інтерактивним повзунком: його не можна перетягнути та змінити час. Позиція відповідає секундам поточного київського дня, обмежена краями шкали. Стани: до початку, триває пара, перерва, пари за сіткою завершені. Повні номери й часові межі пар/перерв доступні в розгортаному переліку; вузькі відрізки не втрачають доступного опису.

Кольори обирає адміністратор у чинному розділі «Навчальні пари». За уточненням VID-011 після релізу `a178039` нативне поле `input type="color"` дозволяє довільний непрозорий RGB-колір; це виняток тільки для позначень пар, без зміни бренду чи primary. Початкові вісім кольорів збережено; повторення дозволене. Кольори впливають лише на шкалу й позначку в довіднику. Історичний журнал не змінюється. Нова можливість потребує міграції 012, ще не застосованої до робочої БД.

## План

- [x] Зафіксувати вимоги та тестові межі.
- [x] Додати валідоване поле кольору, безпечну міграцію й керування адміністратором.
- [x] Винести розрахунок сегментів, київського часу й позиції маркера в окремий модуль.
- [x] Додати шкалу під публічною навігацією; таймер з очищенням і оновленням при поверненні до вкладки.
- [x] Підготувати поведінкові тести та інтеграційний сценарій ізольованої БД.
- [x] Незалежний пакетний QA: unit, integration, typecheck, build, browser, `ui-spacing-review`.
- [x] За запитом VID-009 застосувати перевірену міграцію робочої БД та опублікувати.

## Тестові межі

Обрані за постійним дозволом на `tdd`; підготовка сценаріїв не означає виконаний red → green цикл.

1. Публічний модуль розрахунку шкали: пропорції пар/перерв, сортування за часом, неактивні слоти, точні межі, порожня сітка, київська північ і літній/зимовий час. Час передається аргументом.
2. Правила пар: дозволені кольори, нормалізація, відхилення недопустимих значень, незмінні перевірки часу/перетинів.
3. Реальні Server Actions + читання довідника в ізольованій БД: лише адміністратор створює/змінює колір; reload повертає збережене; деактивація прибирає пару зі шкали; без доступу до робочих даних.
4. Браузер у фінальному QA: рух часу без reload, перерва та крайові стани, повернення вкладки, збереження кольору, розкривний перелік з клавіатури, відсутність hydration/console помилок і overflow на 1440/820/390 px; перевірка контрасту й reduced motion.

Жодних production-міграцій чи деплою в межах реалізації.

## Реалізовані частини

- `lib/class-periods/colors.ts`: єдина клієнтська/серверна палітра, нормалізація кольору та контрастний текст сегмента.
- `lib/class-periods/rules.ts`, `repository.ts`: обов’язковий валідований колір; чинні перевірки номера/часу/перетинів збережені.
- `/dashboard/periods`: створення й редагування кольору зі зразком; Server Actions перевіряють адміністратора перед записом.
- `lib/class-periods/timeline.ts`: одна функція розраховує позицію, стани, перерви й сегменти для заданих пар і часу; UI не зберігає дубльований стан прогресу.
- `components/public-day-timeline.tsx`: серверне читання актуального довідника, окремий стан помилки, без персональних даних.
- `components/day-timeline.tsx`: початковий серверний timestamp для узгодженої гідратації; локальний годинник щосекунди, без щосекундних запитів до БД. При поверненні до вкладки час перераховується, при демонтажі таймер і слухачі прибираються. Кольори/час довідника оновлюються при наступному відкритті або reload сторінки, не через фоновий live-sync.
- `components/public-header.tsx`: шкала під навігацією на головній та публічному розкладі; завантаження через Suspense.
- `app/globals.css`: лише канонічні кольори/токени; пропорційні сегменти, тонкий маркер, зручний перелік часових меж, адаптивні поля адміністратора. Глобальне reduced-motion правило прибирає плавний рух, не вимикаючи оновлення часу.

Міграція `db/migrations/011_class_period_colors.sql` додає `class_periods.color` з обмеженням палітри. Старі записи без кольору заповнюються за номером (цикл із 8 кольорів); повторний запуск не перезаписує налаштування. Нові SQL-записи мають default бірюзовий; форма адміністратора дає свідомо обрати колір. Часові слоти, заняття та історія журналу не переписуються. Для всього накопиченого коду потрібні **009 → 010 → 011**, лише після дозволеного QA та погодженої міграції робочого середовища.

## Підготовлені перевірки

- `lib/class-periods/timeline.test.ts`: частки тривалості, секунди, межі пар/перерв, сортування, неактивні записи, північ, літній/зимовий час, порожня й одинична сітка.
- `lib/class-periods/colors.test.ts`: палітра, недопустимі значення та розрахунок контрасту номерів.
- `lib/class-periods/rules.test.ts`: валідний/невалідний колір разом із попередніми правилами.
- `info/testing/class-periods.integration.test.ts`: реальні авторизовані дії, збереження/нормалізація, кольори публічної шкали, деактивація, дублікати/перетини, відмова трьом стороннім ролям.

Виконана команда незалежного QA (PASS, лише ізольована схема):

```powershell
node --env-file=.env.local info/testing/attendance-runner.mjs --periods
```

Harness застосовує 011 лише до окремої тестової схеми. QA додатково перевіряє первинний backfill і повторний запуск міграції на фіктивних старих записах, рух маркера на межі пар/перерв і доби, responsive-поведінку коротких сегментів, правдиві loading/empty/error стани та чисту консоль. Жодних вигаданих browser-вимірювань на етапі розробки.

## VID-011 — компактний вибір кольору

### Актуальне уточнення: input type="color"

28.08.2026 користувач попросив нативне поле кольору замість восьми зразків. **Реалізовано; QA очікується (NOT_RUN)**. Попередні Popover/radio та стрілка прибрані; у таблиці залишено 44×44 px color input з доступною назвою відповідної пари. Діалог вибору надає браузер / ОС. `color` надсилається рівно один раз прихованим полем своєї форми; видимий picker без `name` / `form` не підпадає під native reset при невдалому action. Після успішного створення попередній key-remount повертає початковий бірюзовий. Явне збереження, pending, ізоляція рядків і права не змінені.

`parsePeriodColor` приймає лише шість HEX-цифр із `#` і повертає uppercase; існуючі значення сумісні. `periodColorForeground` зберігає колишні підписи для початкової палітри та добирає білий / чорнильний / чорний текст для інших кольорів із контрастом не нижче 4.5:1. Не допускаються alpha / назви CSS / `url()` / довільні вирази.

Міграція `012_class_period_custom_colors.sql` атомарно замінює тільки `class_periods_color_allowed` на перевірку `#RRGGBB`, без UPDATE записів; default і NOT NULL зберігаються. Файл 011 не редагується. **012 потрібно погоджено застосувати до запуску нового коду у відповідному середовищі**; до цього БД відхилятиме нові кольори. Під час цієї реалізації жодної міграції / тесту / build / браузера / push / deployment не виконано.

Підготовлені сценарії за наявними публічними TDD-межами: parse / validate довільних і некоректних HEX, контраст чорного/білого/сірого та вибірки RGB; реальні actions→читання довідника→публічний timeline з новими кольорами; збереження часових конфліктів і ролей. Harness додасть 012 лише до власної ізольованої схеми під час наступного QA. Міграційний сценарій перевірить перехід із 011, збереження даних, повтор 012, malformed/NULL SQL і календар.

Фінальна UI-черга: миша/touch/клавіатура в підтриманому native picker, відсутність автоматичного submit при виборі, скасування picker, доступний підпис, один `FormData.color`, незалежні рядки, default і custom create, reset після success, збереження після reload, помилка/pending; 1440/820/390, відсутність document overflow, незмінний бренд. Поведінка системного picker може різнитися між браузерами; не підмінювати її тестом старого Popover. Нижче — історія попередньої реалізації.

Новий запит після релізу: «тут просто вибір кольору без назви». Це зміна адміністративного поля, не нова палітра й не зміна публічної шкали. Видимі назви кольорів прибираються; назви для читачів екрана зберігаються.

План: винести поле в локальний компонент, використати наявний Base UI Popover для позиціювання, Escape / outside-close та фокусу; показати один зразок 44 × 44 px і палітру 4 × 2 із нативними radio-контролами. Вибране позначається контрастною галочкою з `PERIOD_COLORS.foreground`. Підпис «Колір шкали», палітра й бренд незмінні. На час відправлення поле недоступне. Прихований input `color` залишається всередині вихідної форми, палітра — у portal; radio-групи мають унікальні імена та не підмінюють form payload.

Перевірки для фінального QA: створення зі стандартним і зміненим кольором, reset після успішного створення, редагування / reload, помилка збереження, незалежність кількох рядків, активація / деактивація, єдине `FormData.getAll('color')` значення. Використати наявні доменні та ізольовані інтеграційні сценарії пар; нової серверної тестової межі не потрібно. У браузері — миша, touch, Enter / Space / стрілки / Escape, повернення фокусу, доступні назви й позначка вибору, 1440 / 820 / 390 px та popup біля країв екрана. Фактичні вимірювання відкласти до `ui-spacing-review` незалежного QA.

Реалізовано в `app/(private)/dashboard/(admin)/periods/period-color-field.tsx` та `period-color-field.module.css`; спільне поле використовується у створенні й редагуванні через `period-manager.tsx`. Доступний підпис кнопки містить поточний колір, видимий текст із назвою / HEX відсутній. Попередні ключі reset збережені. Примітив Popover уже був у залежності `@base-ui/react`; нових пакетів немає. Спільний `.period-color-swatch` публічної шкали не змінений.

Стан VID-011: **Реалізовано; незалежний QA PASS** — [QA-20260828-02](qa/reports/QA-20260828-02.md). Підтверджені keyboard / focus, payload із рівно одним color, reset, save / reload, pending та popup на 1440 / 820 / 390. Після табличного refactor VID-012 прихований input прив'язаний до відповідної форми через `form`, а pending передається з її useActionState; видимий підпис замінює заголовок колонки. Це не test-first red → green цикл. Весь пакет ще BLOCKED на окремій zoom-перевірці VID-010; deployment і push заборонені.
