# Доручення незалежному QA-агенту: QA-20260902-03

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`, `.agents/skills/qa-batch/SKILL.md`, `info/qa/process.md`, `.agents/skills/ui-spacing-review/SKILL.md` та задачі `VID-030`–`VID-032` у `info/tasks.md`. Ти вже QA-агент: не делегуй цей QA іншому агенту.

- Тригер: підготовка прямо дозволеного production-релізу на `web-dev.pp.ua` з подальшим імпортом розкладу.
- Пакет: `QA-20260902-03`.
- Задачі: `VID-030`, `VID-031`; для `VID-032` перевірити release readiness і відкрити/закрити pre-release gate, не виконуючи зовнішні production-кроки.
- Первинні запити:
  - VID-030: «прибери» — прибрати верхню публічну сітку пар, перекреслену на скриншотах.
  - VID-031: «Давай не так використай ось це за основу `D:\morpz\my-schedule`, але додай адмінку і щоб показувався список груп».
  - VID-032: «задеплой на `https://web-dev.pp.ua/` і щоб був одразу імпортований розклад».
- База останнього production-релізу й поточний `HEAD`: `48fb83e52f5795ee84410ad2f5c791f1eb2beb0c`.
- Перевірюваний стан: `HEAD` плюс усі поточні modified/untracked файли. Продуктовий diff містить публічну сторінку розкладу, explorer/CSS, серверний список активних пар, тест межі груп/пар і документацію. Самостійно сформуй точний manifest та SHA-256 без env/секретів.
- Відкладених продуктових задач у межах цього пакета немає. Реальні deployment, push, production migration та production import не виконувати й не позначати як фактично завершені.
- Середовище: використовуй наявний ізольований PostgreSQL harness `pnpm test:schedule-v2-integration`; не змінюй робочі дані й не публікуй значення env.
- Режим: лише QA; продукт не редагувати, push/deploy/production migration/production import заборонені.
- Дозволені записи: QA-звіт, QA-поля `info/tasks.md`, QA-артефакти та необхідні тести в узгодженому обсязі.

Самостійно класифікуй запити, склади один ризик-орієнтований план і перевір весь пакет. Обов’язковий мінімум: `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build`, `pnpm test:schedule-v2-integration`, diff/secret/generated scan. Для UI перевір у production-mode browser `/schedule` на 1440, 820 і 390 px: список і пошук груп, вибраний стан та URL, день/тиждень, навігацію датами, всі активні пари й порожні слоти, поточну пару, паралельні заняття, keyboard/focus/Escape, loading feedback, console, локальний/document overflow. Застосуй `ui-spacing-review` і зафіксуй вимірювання. Статично та через HTTP підтвердь, що `/admin` лишився захищеним, а публічна верхня сітка VID-030 відсутня.

`VID-032` не може бути підтверджений як завершений до зовнішніх операцій; у висновку чітко вкажи, чи дозволяє актуальний стан виконати push/deploy/import. Запиши звіт у `info/qa/reports/QA-20260902-03.md` за шаблоном. Оновлюй лише QA-результати в `info/tasks.md`, зберігаючи первинні запити й історію. Поверни `PASS`/`FAIL`/`BLOCKED`, відбиток коду, що виконано, дефекти, неперевірене та посилання на звіт.
