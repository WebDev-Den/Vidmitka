# Доручення незалежному QA-агенту: QA-20260904-12

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`,
`.agents/skills/qa-batch/SKILL.md`, `info/qa/process.md` та `VID-061` у
`info/tasks.md`. Ти вже QA-агент: не делегуй цей QA іншому агенту.

Тригер: підготовка дозволеного користувачем production-релізу після уточнення
«це прибери».

Пакет: `QA-20260904-12`.

Задача: `VID-061`.

Первинний запит: «я додам в env необхідні дані а ти в адмінці створи вкладку
крон де буде первірятись і писати коли було звернення і кнопка ініціалізувати
крон». Остаточне уточнення: скриншот усієї `/admin/cron`, закреслений червоним,
і запит «це прибери». Це означає прибрати весь видимий розділ Push cron:
навігацію, картку overview, route, форму/Server Action, adapter, тести і стилі.
Не змінювати scanner route, QStash variables у Vercel, зовнішні schedules або
push-підписки.

База: `a50e3a1` (опублікований стан із UI cron).

Перевірюваний стан: поточний `HEAD` плюс dirty diff; не включати старі
QA-звіти/доручення в оцінку продуктового коду.

Виключення: фактичні QStash API calls, створення schedules, production migration
і реальна browser push-підписка. Користувач 04.09.2026 окремо скасував browser,
UI та spacing QA як release gate тільки для VID-061; не виконуй browser QA й
зафіксуй це як погоджене обмеження, а не як непідтверджений позитивний результат.

Середовище: не читати і не друкувати env/secret values; не виконувати запити,
що змінюють production або QStash. Для коду застосунку виконати `pnpm exec tsc
--noEmit`, `pnpm test` і `pnpm build`, а також безпечний diff/secret scan.

Режим: лише QA; продукт не редагувати, push/deploy/production migration заборонені.
Дозволені записи: `info/qa/reports/QA-20260904-12.md` і QA-поля `VID-061` у
`info/tasks.md`; не змінюй первинні запити чи історію.

Самостійно класифікуй запит, склади один ризик-орієнтований план і зістав
фактичний diff із критеріями. Переконайся, що route і всі imports/посилання на
адмінський UI/adapter зникли, а scanner route та public push код лишилися.
Запиши звіт за `info/qa/report-template.md`, поверни результат і чи відкрито
release gate для цього конкретного стану коду.
