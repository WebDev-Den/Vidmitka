# RELEASE-20260828-01 — публікація VID-001–VID-008

Запит VID-009: «перевір залий на гід задеплой щоб все працювало». Цей журнал описує релізні операції основного агента; незалежний висновок QA — у [QA-20260828-01.md](QA-20260828-01.md).

## Передрелізний стан

- Git remote: `https://github.com/WebDev-Den/Vidmitka.git`, гілка `main`.
- Після `git fetch origin main` локальний HEAD і origin/main: `58a3f9151b56041f0e4feebf558ce7a160fe6c4f`, без розходження.
- Vercel: проєкт `vidmitka`, scope `webdevdens-projects`, GitHub production branch `main`.
- Поточний попередній deployment: `dpl_A2uusF3JLz4SShd9Wa9r4ABufB5P`, READY, source Git, той самий SHA.
- Єдиний домен проєкту: `vidmitka.vercel.app`. Колишній `web-dev.pp.ua` не підключати знову.
- Production env доступні: DATABASE_URL, DATABASE_URL_UNPOOLED, ADMIN_EMAILS, ADMIN_REGISTRATION_TOKEN. Значення не виводилися та не включені до Git.
- Read-only перевірка production-БД: застосовані міграції 001–008. Для нового коду потрібні 009–011.
- Контрольні кількості до міграцій: 1 користувач, 0 занять, 0 студентів, 0 журналів, 8 пар. Робочі дані не очищалися.
- Налаштованих Vercel Drains немає; `@vercel/analytics` уже підключений. Після публікації потрібна read-only перевірка runtime logs; зовнішній збір логів не налаштований.

## Послідовність і поточний статус

1. Незалежний QA: остаточний PASS після закриття QA-01–QA-04. TypeScript, 199 unit-тестів, build, 7 DB-сценаріїв і production-mode browser пройшли. Перед релізом основний агент повторно підтвердив точний fingerprint `9d9d5fb977d94596f48322ce939c274c71b2baadca94c407040e97d909b23e09` (132 файли).
2. Міграції 009 → 010 → 011 застосовані штатним runner із production env після перевірки точного Neon host/database. 001–008 пропущені як уже застосовані; схема й дані не очищалися.
3. Закомітити весь перевірений пакет без секретів/generated artifacts, push origin/main. Нативний Vercel gate: `pnpm test && pnpm build`.
4. Підтвердити READY саме для нового Git SHA, production alias і доступність головної/розкладу/авторизації, захист приватних route без записів у робочу БД.
5. Не раніше 60 секунд після READY перевірити ранні runtime errors через bounded `vercel logs --no-follow`. Відсутність помилок підтверджує лише фактично перевірене вікно, не майбутню безвідмовність.

Production-міграції виконані. Повторне читання підтвердило всі 001–011; контрольні кількості незмінні (1 користувач, 0 занять/студентів/журналів, 8 пар). Додано 3 початкові типи; дат відпрацювань 0, довільних дат не створено. Для всіх восьми пар установлені дозволені кольори. Перед commit перевірено 140 staged-файлів: секретів із локального env, заборонених generated paths і whitespace errors не знайдено.

Commit, push і новий deployment ще очікуються; після них потрібне фактичне підтвердження READY та live smoke.
