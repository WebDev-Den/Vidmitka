# QA-20260828-03 — доручення перед релізом

Тригер: запит «перевір і залий» (VID-018). Перевірюваний пакет VID-010–VID-017, координація VID-018. Точні запити й критерії — відповідні розділи `info/tasks.md`; прочитати також пов'язані плани UI / функцій. Попередній QA-20260828-02 — історичні докази, не PASS нового diff. Незакритий browser200% VID-010 потрібно фактично перевірити без обходу policy guard.

База production: `efcc141`; поточний HEAD `ac91a8cca5750d949d688d79606d379683aed780`, повний dirty/untracked обсяг. Зафіксувати список і контрольні суми коду, тестів та конфігурації. Сторонніх змін наразі не визначено; секрети / env / generated artifacts виключити.

Роль: незалежний QA, модель gpt-5.6-sol / high через права і міжмодульні зміни календаря / списків студентів. Прочитати AGENTS.md, qa-batch, process, report-template та необхідні UI/browser skills. Самостійно класифікувати запити й зіставити критерії з поведінкою, не довіряти статусу «реалізовано».

Один пакетний прогін: pnpm exec tsc --noEmit, pnpm test, pnpm build; релевантні DB integration у штатному harness з ізольованою схемою / роллю. Нові flags --directories і --deferred-roster, змінений --calendar; наявні групи / журнал / довідники / парні конфлікти / адміністраторські сценарії — за ризиком. Звичайні skipped DB tests не вважати PASS.

Browser: локальний production build з ізольованими фіктивними даними. Основні CRUD та нові flow, негативні / рольові сценарії, клавіатура, pending, console, ui-spacing-review1440/820/390, реальний200%. Не використовувати локальний env як доказ ізоляції: він може вказувати на production. Використовувати штатну схему / роль harness, не робочі записи. Після завершення прибрати лише власні процеси / схеми.

Дозволено: звіт `info/qa/reports/QA-20260828-03.md`, QA-поля задач, необхідні тестові сценарії. Продукт не редагувати, не комітити, не пушити, не деплоїти, не міняти робочі дані чи production-схему. Повертати дефекти основному агенту пакетом; він виправляє й передає повторно.

Повернути PASS / FAIL / BLOCKED, докази, відбиток, невиконані перевірки та релізний висновок. Не створювати іншого QA-агента. Основний агент під час перевірки лише готує реліз і read-only перевіряє Git / Vercel.

## Пакет виправлень першого прогону

Користувач уточнив «далі»; це продовження VID-018, не нова задача і не скасування перевірки200%. Після checkpoint QA основний агент усуває чотири підтверджені дефекти:

1. QA-03-01 / VID-014: доступна назва всіх трьох пошукових popup через спільний компонент, без зміни пошуку / створення.
2. QA-03-02 / VID-012: режим групи передавати єдиним hidden input з React-стану; видимий контроль поза form owner, щоб native reset не змінював його всупереч умовному полю. Після reset назви очищені, вибраний спосіб збережений.
3. QA-03-03 / VID-013: компактні стилі sidebar діють лише на desktop від861px; назва бренду й активний chevron лишаються в DOM і приховуються лише відповідним CSS. Мобільний drawer завжди має підписи, навіть після згортання на desktop; зворотний resize зберігає desktop-вибір.
4. QA-03-04 / VID-015: текст порожнього журналу з посиланнями обгорнути одним абзацом усередині notice, щоб flex не розділяв inline-текст на колонки; без глобальних змін notice / приховування overflow.

Повторний QA: один tsc / unit / build; три popup з доступними назвами, keyboard / focus / create і помилками; два послідовні створення студентів у нових групах, перемикання на наявну, reset і single groupMode payload; desktop collapsed→390/820 drawer→desktop зі збереженням підписів / focus / pending; порожній журнал із посиланнями без overflow1440/820/390. Завершити невиконані критерії першого прогону; незмінені DB-результати не підміняти повторними зайвими прогонами. Продуктові й QA-статуси оновлювати окремо.
