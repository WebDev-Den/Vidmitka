# Доручення QA-20260902-07

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`, `.agents/skills/qa-batch/SKILL.md`, `info/qa/process.md`, `info/qa/report-template.md`, `.agents/skills/ui-spacing-review/SKILL.md` та задачі VID-038–VID-041 в `info/tasks.md`. Ти вже QA-агент: не делегуй цей QA іншому агенту.

- Тригер: прямий запит користувача «перевір і задеплой»; QA перед дозволеним релізом.
- Пакет: `QA-20260902-07`.
- Задачі: VID-038, VID-039, VID-040; VID-041 є release orchestration.
- Первинні запити: «мені не потрібно шо там імпортоване і чому це разове заннятя вого ж по розкладу до 31 грудня»; «прибери» з перекресленим перемикачем «День / Тиждень»; «тут має бути чисельник знаменник»; «перевір і задеплой».
- База: production commit `2a75244`.
- Перевірюваний стан: `HEAD` `2a75244` плюс увесь tracked dirty та untracked scope на момент старту. Сформуй manifest/checksums без env і секретів; QA-звіт і QA-поля журналу не включай у fingerprint продукту.
- Очікувані application-файли: public schedule page/explorer/CSS; week settings page/form; private schedule manager; import parser/repository/public resolver/calendar override module; parser та isolated schedule-v2 integration tests. Очікувані info-файли: task ledger, recurring import contract, mapping, operations, data-model/calendar docs та README.
- Виключення: історичні QA/release звіти й уже опубліковані VID-001–VID-037 не переперевіряти поза потрібними регресіями. Не виконувати production reimport, migration чи інші записи.
- Середовище: використовуй наявний ізольований schedule-v2 PostgreSQL harness. Не виводь значення env/секретів. Browser QA запускай лише над власною isolated схемою/процесом і прибери їх після прогону.
- Режим: лише QA; продукт не редагувати, commit/push/deploy/production migration заборонені. Дозволені записи: QA-звіт, QA-поля VID-038–VID-041, необхідні QA-тести в узгодженому обсязі.

Самостійно класифікуй запити, склади один ризик-орієнтований план і перевір пакет. Мінімум: `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build`, `pnpm test:schedule-v2-integration`; перевір фактичну кількість виконаних/skipped тестів. Для UI перевір у браузері public `/schedule` та admin week settings / schedule на 1440, 820, 390 px: відсутність «День / Тиждень», денна навігація й фільтр, відсутність технічних import badges, назви «Чисельник / Знаменник», keyboard, console, document overflow. Застосуй `ui-spacing-review` та зафіксуй вимірювання.

Для імпорту підтвердь: 431 `schedule_entries` до 31.12.2026, idempotency/update/rollback, legacy exception projection без change metadata, reimport migration до entry + `superseded`, справжній `one_time` без регресії, календарний transfer. Не роби жодних production-write.

Запиши звіт у `info/qa/reports/QA-20260902-07.md`, онови лише QA-результати VID-038–VID-041 у `info/tasks.md`, збережи первинні запити та історію. Поверни PASS/FAIL/BLOCKED, перевірений fingerprint, виконане, дефекти/неперевірене, cleanup і посилання на звіт. QA-gate відкривай лише для повного актуального PASS.
