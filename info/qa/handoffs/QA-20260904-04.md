# Доручення QA-20260904-04

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`, `.agents/skills/qa-batch/SKILL.md`, `.agents/skills/ui-spacing-review/SKILL.md`, `info/qa/process.md`, `info/qa/report-template.md`, `info/tasks.md` (VID-051–VID-054) і повні інструкції потрібного browser/Playwright skill. Ти вже QA-агент: не делегуй цей QA іншому агенту.

- Тригер: прямий запит користувача «…перевір і задеплой» після остаточного уточнення прибрати Web Push і потребу в `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `CRON_SECRET`.
- Пакет: `QA-20260904-04`.
- Задачі: VID-051, VID-052, VID-053, VID-054.
- Первинні запити й критерії: точні формулювання та повні критерії у відповідних розділах `info/tasks.md`; не довіряй записаним статусам реалізації.
- База: production commit `ab5e964388fbabdecb8b19d6081bf48d57259eae`.
- Перевірюваний стан: `HEAD` на цій базі + весь поточний dirty/untracked продуктовий пакет VID-051–VID-053. Зафіксуй ordered перелік і SHA-256 implementation/test-файлів без env/секретів; QA-документи не включай у продуктовий fingerprint.
- Виключення: завершені VID-001–VID-050 як окремі задачі, крім потрібної регресії спільного публічного розкладу; production DB writes, production migrations, commit, push і deployment.
- Середовище: чинний локальний Next/Vitest/browser harness та ізольований schedule-v2 DB harness. Не виводь значення локальних секретів.
- Режим: лише QA. Продукт і тести не редагувати. Дозволені записи: QA-звіт, QA-артефакти та QA-поля VID-051–VID-054 у журналі.

Самостійно класифікуй задачі, склади один ризик-орієнтований план і зістав кожен критерій із фактичним результатом. Обов'язково:

- виконати `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build`, `pnpm icons:check` та релевантний `pnpm test:schedule-v2-integration` тільки в його ізольованій схемі;
- підтвердити регресію VID-051: canonical `/`, legacy redirect, незмінний URL під час керування, cookie викладача, сьогодні Europe/Kyiv після reload, abort/race/error, transfers handoff, fixed shell, внутрішній scroll, initial current/next/last target і кнопка повернення;
- перевірити VID-052: `/manifest.webmanifest`, `start_url=/`, `scope=/`, standalone, українська мова, 192/512/maskable icons, їхні фактичні розміри, `/sw.js` з JavaScript MIME, `no-store`, власним CSP і без кешування/push; реєстрацію service worker; install control, `beforeinstallprompt` наскільки дозволяє браузер, fallback/iOS/standalone стани та доступну назву;
- статично й у зібраному продукті підтвердити відсутність bell/modal, `Notification`/`PushManager`, push routes, cron, push DB migrations, `web-push` dependency і вимоги до перелічених VAPID/CRON environment variables; історичні згадки у `info/` є доказом вилученої вимоги, а не дефектом;
- перевірити VID-053: click/keyboard номера пари, loading/error/retry/empty/result, дата/номер/час, повний перелік активних вільних аудиторій незалежно від teacher filter, скасовані заняття, неактивні аудиторії, Escape, focus trap/return і незмінний URL;
- виконати реальний browser/UI QA та `ui-spacing-review` на 1440, 820 і 390 px: усі основні 44 px controls, header/grid density, modal/popover containment, keyboard/focus, `prefers-reduced-motion`, console без нових application errors/warnings і відсутність document-level horizontal overflow; окремо low-height/200%-proxy для fixed-shell регресії;
- переконатися, що QA не робить записів до production-БД і прибирає лише власні процеси, вкладки та ізольовану схему.

Запиши звіт у `info/qa/reports/QA-20260904-04.md` за шаблоном. Онови лише QA-результати/статуси VID-051–VID-054, зберігаючи первинні запити та історію. Зупинись після завершеного прогону; дефекти поверни основному агенту. У висновку дай `PASS`, `FAIL` або `BLOCKED`, перевірений fingerprint, виконане, неперевірене й стан QA-gate. Успішний QA не є дозволом агенту на push/deployment; дозвіл користувача реалізує основний агент лише після твого актуального `PASS`.
