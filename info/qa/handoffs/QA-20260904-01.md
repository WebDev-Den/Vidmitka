# Доручення QA-20260904-01

Твоя роль — незалежний QA-агент Vidmitka. Прочитай `AGENTS.md`, `.agents/skills/qa-batch/SKILL.md`, `info/qa/process.md`, `info/qa/report-template.md`, `.agents/skills/ui-spacing-review/SKILL.md` та задачу VID-046 у `info/tasks.md`. Ти вже QA-агент: не делегуй цей QA іншому агенту.

- Тригер: підготовка дозволеного користувачем production-релізу («задеплой»).
- Пакет: `QA-20260904-01`.
- Задача: `VID-046`.
- Первинний запит: «зроби щоб було на всю ширину».
- База останнього production-релізу: commit `3a3697e`.
- Перевірюваний стан: `HEAD 3a3697e` + dirty зміни в `app/globals.css`, `components/public-schedule-explorer.module.css`, `info/public-schedule-responsive-layout.md`, `info/tasks.md`; цей handoff і майбутній QA-звіт не входять у fingerprint продуктового коду.
- Виключення: завершені VID-001–VID-045 і будь-які сторонні файли поза вказаним diff.
- Середовище: локальний застосунок; production-БД може бути підключена через env, тому лише read-only browser-сценарії, без reimport, міграцій чи записів.
- Режим: лише QA; продуктовий код не редагувати, commit/push/deploy/production migration заборонені.
- Дозволені записи: `info/qa/reports/QA-20260904-01.md` та QA-поля VID-046 у `info/tasks.md`.

Самостійно класифікуй запит і склади один ризик-орієнтований план. Зістав критерії VID-046 з фактичним CSS та UI. Виконай мінімум `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build`. Оскільки змінено UI, перевір у реальному браузері ширини 1440, 820 та 390 px, застосувавши `ui-spacing-review`: фактичну ширину header/workspace, поля, вирівнювання, корисне використання широкого viewport, console, keyboard-регресію основних контролів і відсутність document-level horizontal overflow. Для критерію «без обмеження 1480px» додатково перевір viewport ширший за 1512 px (наприклад 1920 px) і зафіксуй browser-вимірювання.

Запиши звіт у `info/qa/reports/QA-20260904-01.md` за шаблоном і онови лише QA-результат VID-046 в `info/tasks.md`, зберігаючи первинний запит та історію. Поверни `PASS`, `FAIL` або `BLOCKED`, перевірений fingerprint, виконані й невиконані перевірки та посилання на звіт.
